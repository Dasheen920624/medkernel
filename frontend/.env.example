# 前端环境变量样例（Vite 约定：以 VITE_ 前缀的变量会被注入到 import.meta.env）
# 真实 .env 不进 Git，请按本机/部署环境复制本文件为 .env.local 后修改。

# 开发期 vite proxy 的真实后端目标；必须显式配置，避免误连旧服务。
VITE_API_PROXY_TARGET=http://localhost:18080
E2E_API_BASE_URL=http://localhost:18080/medkernel/api/v1
