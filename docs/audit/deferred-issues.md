# 待处理问题清单

> 用途：登记当前阶段无法真实完成、但不应阻塞长期主线继续推进的问题。本清单是长期任务的统一待处理问题清单；登记不等于降低质量标准，进入对应负责阶段时，必须按关闭证据逐项清零。

## 执行原则

1. 影响医疗安全、登录可用、权限隔离、真实性门禁、当前卡主链路的缺陷不得延期。
2. 外部环境、闭源驱动、客户现场资源等非当前阶段可控问题，登记后不阻塞后续卡领取。
3. 已登记问题不得被写成“已通过”；只能写“已登记、待对应阶段处理”。
4. 后续 AI 领取任务时先读 [\_HANDOFF](../_HANDOFF.md)，再核对本清单是否有归属到当前阶段的问题。
5. 长期目标执行中遇到 `open` 项默认登记后继续推进，不等待、不空转，也不得把长期目标标记为 `blocked`；只有触及当前卡主链路、登录可用性、权限隔离、真实性门禁或医疗安全红线时，才暂停领取下一阶段并即时处理。
6. 新增问题必须在发现当轮写入本清单，分配 `DEFER-XXX`，补齐影响、归属阶段和关闭证据；不得只写在会话记录里。
7. 每个阶段收尾时必须复核本清单：归属当前阶段的问题要转 `in_progress` 并关闭；不归属当前阶段的问题保持 `open`，继续主线。

## 状态定义

| 状态        | 含义                     |
| ----------- | ------------------------ |
| open        | 已登记，等待对应阶段处理 |
| in_progress | 当前阶段正在处理         |
| done        | 关闭证据已提交并验证     |

## 长期任务登记流程

1. 发现问题先判定归属：若触及当前卡主链路、登录可用性、权限隔离、真实性门禁或医疗安全红线，当轮处理；否则进入本清单。
2. 登记必须写清 `ID / 影响 / 当前是否阻塞 / 处理阶段 / 关闭证据`，关闭证据必须可复现、可审计、不可只写“人工确认”。
3. 登记完成后继续当前阶段任务，不等待外部环境、闭源驱动、客户现场资源或院方系统资源；这类问题不构成长期目标阻塞。
4. 后续 AI 每次开工必须先读 [\_HANDOFF](../_HANDOFF.md) 和本清单；领取到对应处理阶段时，再把相关 `open` 项转为 `in_progress` 并提交关闭证据。

## 当前清单

| ID        | 问题                                                                                                                                                     | 当前影响                                                                                                                                                                                              | 当前是否阻塞 | 处理阶段                                                                            | 状态 | 关闭证据                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| --------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------ | ----------------------------------------------------------------------------------- | ---- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| DEFER-001 | 达梦 / 人大金仓 + 国产 OS / JDK 真实运行环境适配                                                                                                         | 不影响当前 PostgreSQL + Oracle 范围、D0 收口和后续阶段领取；不得宣称国产化真实环境已通过                                                                                                              | 否           | D6 `DOMCHK-01` 页面真实探测；GA `QA-02` / `INFRA-10` 总验收                         | open | 在真实国产化环境运行 `deploy/docker/scripts/govcloud-smoke.sh`，提交不含口令且 `status=PASS` 的 `govcloud-smoke-*.txt`、OS/JDK/JDBC 驱动 SHA-256、CI / 验收记录                                                                                                                                                                                                                                                                                                |
| DEFER-002 | 前端构建工具链依赖审计曾存在 7 个开发依赖告警（5 moderate + 2 critical），工程预演复核又发现 `form-data` 生产依赖 high 漏洞 | 已关闭：升级 `form-data`、Vite、Vitest、React 插件和覆盖率工具，并以 `js-yaml` 安全版本覆盖收口传递依赖；未使用 `npm audit fix --force`，生产与开发依赖审计均为 0。 | 否           | INFRA 依赖治理专项；GA `INFRA-10` 总验收前                                          | done | commit `13b69304`；`npm audit --audit-level=moderate` 与 `npm audit --omit=dev --audit-level=moderate` 均退出 0；前端 99 文件 / 795 测试、typecheck、eslint 零 warning、stylelint、lint 规则测试、format、Vite 6.4.3 build 全部通过；工程预演聚合仍须保持 `PASSED/REHEARSAL_READY`。 |
| DEFER-003 | 前端测试与构建输出存在非阻断噪声：React Router v7 future flag、Antd/rc-menu `act(...)`、React Query undefined 数据告警、`vendor-antd` chunk 大小提示     | 不影响当前主线；真实浏览器页面不得有运行时错误，当前告警不能被写成已消除                                                                                                                              | 否           | INFRA-01 / SYS-07 / GA `INFRA-10` 体验与性能收口                                    | open | 启用或适配 Router future flags，修正测试 harness / React Query 默认数据，拆分或明确大 chunk 策略；前端全量测试与构建输出无该类告警，并附浏览器验收记录                                                                                                                                                                                                                                                                                                         |
| DEFER-004 | 本机浏览器自动化能力曾不可稳定使用；2026-06-07 P8-2 已恢复 in-app Browser 验收，但项目 Playwright 的 headless/full Chrome 截图链路仍需工具链收口 | 已关闭：新增项目 Playwright 截图链路，覆盖 `/login`、登入后 Header 用户菜单、`/rule/definitions` 桌面与 `390px` 窄屏、`/config/packages` 发布弹窗；链路同时采集浏览器错误、HTTP 失败响应和网络失败，运行时记录为空错误。 | 否           | INFRA-10 工具链验收与本地浏览器插件核查                                             | done | 证据：`frontend/e2e/b0-screenshot-chain.spec.ts`；`cd frontend && E2E_API_BASE_URL=http://127.0.0.1:18082/medkernel/api/v1 MEDKERNEL_API_PROXY_TARGET=http://127.0.0.1:18082 VITE_API_PROXY_TARGET=http://127.0.0.1:18082 npx playwright test e2e/b0-screenshot-chain.spec.ts --project=chromium` 1 passed，生成 `b0-login-desktop`、`b0-header-user-menu`、`b0-rule-definitions-desktop`、`b0-rule-definitions-390px`、`b0-config-packages-release-modal` 和 `b0-screenshot-chain-runtime-records` 附件；同时修复 `ConfigPackages` 打开发布局前调用 `syncForm.setFieldsValue` 导致的 AntD useForm 警告，`cd frontend && npx vitest run src/pages/tenant/ConfigPackages.test.tsx` 21/21 通过。                                                                                                                                                                                                                                                                       |
| DEFER-005 | 真实院方 IdP（OIDC/CAS/SAML/国密 CA）连接器、JWKS/证书链与非对称生产验签环境缺失                                                                         | AUTH-01 已交付 `auth.mode` 配置中心切换、委托登录状态 / 回调挂点与 `NOT_CONNECTED` 诚实降级；不影响平台账号登录、httpOnly+CSRF、审计和当前 PostgreSQL/Oracle 主线。不得宣称院方 IdP 已真实登录成功    | 否           | AUTH-03 凭证安全强化、D5 `IDBIND-01` 身份绑定 / Provider 运维、GA `INFRA-10` 总验收 | open | 在真实或院方认可的 IdP 沙箱完成 OIDC/CAS/SAML/国密 CA 回调，提交不含密钥的配置、JWKS/证书指纹、同一 Resource Server 验签证据、登录成功/失败审计、端到端截图和 CI/验收记录                                                                                                                                                                                                                                                                                      |
| DEFER-006 | 历史迁移中文 COMMENT 覆盖缺口：`check-comment-zh.sh --mode=full` 暴露 oracle/postgres/kingbase 的 V1/V3/V7/V10 存量 GAP                                  | 当前新增 V48 已补齐中文 COMMENT 且 PostgreSQL/Oracle 当前运行验证通过；历史 GAP 不影响当前主线和 changed 门禁。不得宣称历史迁移 COMMENT 全量清零，也不得直接改旧迁移破坏 Flyway checksum              | 否           | BASE-05 迁移治理专项；GA `INFRA-10` 总验收前                                        | open | 制定不破坏已部署 checksum 的修复方案：通过新补偿迁移或经批准的基线重建补齐中文 COMMENT；提交 H2/PostgreSQL/Oracle 迁移验证、`scripts/check-comment-zh.sh --mode=full` 无 GAP 或有批准豁免记录、迁移规约门禁和 CI 证据                                                                                                                                                                                                                                          |
| DEFER-007 | 非当前卡触碰范围的历史页面可见技术化文案残留：如“物理沙盒 / 物理入库 / 物理投影 / 物理合并”等面向用户不自然表达                                          | 本轮已清理 WORKBENCH-01 触碰的工作台与租户生命周期面板；其余残留分布在 D2–D6 多个后续页面，不影响当前工作台 PR1 的真实数据源、权限态和降级主链路。不得把这些后续页面写成体验文案已清零                | 否           | 对应页面卡实施时同步清理；GA `INFRA-10` 体验总验收前全量收口                        | open | 对 `frontend/src/pages` 和 `frontend/src/features` 运行可见技术文案扫描，消除不符合角色语言的“物理”等历史表达或给出医疗/工程语境必要性；提交对应页面测试、浏览器验收截图和全仓扫描证据                                                                                                                                                                                                                                                                         |
| DEFER-008 | 全局缺上下文错误码命名别名未统一：D2 API-01 卡写 `CONTEXT_MISSING`，现有平台脊柱仍返回 `ENG-BASE-001 / TENANT_CONTEXT_MISSING`                           | 不影响当前 API-01 主链路：无租户上下文仍诚实返回 ProblemDetail，跨组织拒绝已新增 `ORG_SCOPE_DENIED / ENG-BASE-004`；不得宣称 `CONTEXT_MISSING` 别名已完成                                             | 否           | BASE-03 错误码契约治理；GA `INFRA-10` 总验收前                                      | open | 统一错误码命名策略并补兼容映射：提交 `CONTEXT_MISSING`/`CONTEXT_VALIDATION_FAILED` 与既有 `ENG-BASE-*`/`ENG-CONTEXT-*` 的规范、全局异常处理测试、前后端错误文案回归和 API 契约文档                                                                                                                                                                                                                                                                             |
| DEFER-009 | API-03 知识资产列表和异步导出真实 10 万级规模的真实方言专项资源占用与 CI/交付证据未完整生成                                                                       | B0 完美化已修复平台主源知识身份列表服务层全量加载风险，并新增 H2 10 万知识身份分页合同；知识异步导出已修复 `filterJson` 未生效问题，并新增 H2 10 万级提交→完成→轮询→下载合同；同时新增 opt-in PostgreSQL 15.18 + Oracle 21.3 Testcontainers 烟测，在 v135 真实方言下构造 10 万知识身份并完成深页查询、导出等价筛选分页扫描、10 万 JSONL 文件生成和导出等价分页 P95 阈值断言。但 PostgreSQL/Oracle 真实 API-03 异步导出资源占用和 CI/交付验收日志尚未关闭，不得宣称 API-03 10 万级全压测已完成 | 否           | D0 `API-13` 大列表治理、GA `SYS-07` NFR 与 `INFRA-10` 总验收                        | open | 已有证据：`mvn -q -Dtest=KnowledgeExportServiceTest,KnowledgeExportServiceLargeScaleTest test`，证明筛选导出与 H2 10 万提交、完成、下载链路；`B0_100K_DIALECT_SMOKE=true mvn -q -Dtest=B0LargeScaleDialectSmokeTest test`，Surefire 2 tests / 0 failures / 0 errors / 0 skipped / 249.002s，PostgreSQL 用例 13.315s、Oracle 用例 235.553s，覆盖 PG/Oracle 知识导出等价筛选扫描、10 万 JSONL 文件生成、导出等价分页 P95 阈值断言（PG <1s，Oracle <5s）和候选/冲突分页 P95 阈值断言（PG <1s，Oracle <5s）。仍需提交 PostgreSQL/Oracle 真实 API-03 异步导出资源占用和 CI/验收日志；若纳入专项压测，应附数据生成脚本与可复现命令 |
| DEFER-010 | API-04 字典映射真实 10 万级候选 / 冲突在真实方言下的专项资源占用与 CI/交付证据未完整生成                                                                            | B0 完美化已新增 H2 10 万标准术语、10 万院内术语、10 万正式映射、10 万候选映射和 10 万冲突记录分页合同；术语候选生成已从全量本地词 × 全量标准词改为分页读取 + 确定性索引召回，并在 V135 改为持久化异步任务，只返回任务、进度、摘要和 `candidatePageUri`，候选明细按 `generationJobCode` 分页查看；同时新增 opt-in PostgreSQL 15.18 + Oracle 21.3 Testcontainers 烟测，在同步 #628 后迁移 v135 并构造 10 万标准术语、10 万院内术语、10 万正式映射、10 万候选映射和 10 万冲突记录，完成深页查询和候选/冲突分页 P95 阈值断言。但 PostgreSQL/Oracle 候选/冲突专项资源占用和 CI/交付验收日志尚未关闭，不得宣称 API-04 10 万级全压测已完成 | 否           | D0 `API-13` 大列表治理、GA `SYS-07` NFR 与 `INFRA-10` 总验收                        | open | 已有证据：`mvn -q -Dtest=TerminologyServiceTest,TerminologyApiContractTest,EngineEndToEndIntegrationTest,TerminologyRepositoryLargeScaleTest test`，证明候选生成异步任务、分页索引和 H2 10 万候选/冲突分页合同；`mvn -q clean -Dtest=MigrationBaselineContractTest,H2BaselineMigrationTest test` 与 `mvn -q -Dtest=FlywayMultiDialectSmokeTest test`，证明 V132/V133/V134/V135 迁移顺序、V135 任务表和 `generation_job_code` 五方言迁移；`B0_100K_DIALECT_SMOKE=true mvn -q -Dtest=B0LargeScaleDialectSmokeTest test`，Surefire 2 tests / 0 failures / 0 errors / 0 skipped / 249.002s，PostgreSQL 用例 13.315s、Oracle 用例 235.553s，覆盖 PG/Oracle 候选/冲突 10 万级深页和候选/冲突分页 P95 阈值断言（PG <1s，Oracle <5s）。仍需提交 PostgreSQL/Oracle 候选/冲突专项资源占用和 CI/验收日志；附数据生成脚本与可复现命令 |
| DEFER-011 | GitHub Actions 仍提示 Node.js 20 actions 弃用，2026-09-16 后将从 runner 移除                                                                             | 不影响当前 PR：CI 8/8 已通过；但后续若不升级 action 运行时，可能在 2026-09-16 后形成 CI 阻塞。不得宣称 CI 工具链弃用风险已清零                                                                        | 否           | INFRA CI 工具链治理；GA `INFRA-10` 总验收前，且最晚 2026-09-16 前                   | open | 升级或替换 `.github/workflows` 中触发 Node.js 20 提醒的 actions，或验证 GitHub runner 默认 Node.js 24 后仍 8/8 通过；提交 CI 8/8 通过记录和无 Node.js 20 deprecation annotation 的 run 链接                                                                                                                                                                                                                                                                    |
| DEFER-012 | 规则影响分析缺少真实跨域反向索引：规则 ↔ 路径模板、规则 ↔ 在径患者、规则 ↔ 发布同步目标尚未建立统一关联                                               | 已关闭：规则影响分析新增关系库只读索引，按路径节点 / 边 JSON 中真实规则引用定位路径模板，按患者路径运行实例定位在径患者，按配置包条目 → 发布计划 → 同步日志 → 同步目标定位发布同步目标；无真实对象时返回空列表，不伪造。 | 否           | D2 `RULE-01` / `SYS-04` 发布框架；D2 域级验收与 GA `INFRA-10` 总验收前              | done | 证据：`RelationalRuleImpactIndex` + `RuleImpactIndexSnapshot`；`PathwayNodeRepository.findByTenantIdAndRuleReference` / `PathwayEdgeRepository.findByTenantIdAndRuleReference` / `PackageItemRepository.findByTenantIdAndAssetTypeAndAssetId`；`mvn -q -Dtest=RelationalRuleImpactIndexTest,RelationalRuleImpactIndexRepositoryTest,RuleEngineServiceTest test` 通过，其中仓库测试在 H2 + Flyway V66 后真实写入路径、患者路径、配置包发布和同步日志并回查；`npm test -- --run src/pages/tenant/RuleDefinitions.test.tsx` 通过，规则发布页展示完整影响对象。 |
| DEFER-013 | OpenSpec 旧变更 `engine-authenticity-remediation` 仍显示 17 项未完成，与当前卡体系 / 已合并 PR 进度不同步                                                | 不影响当前 `RULE-01` 主线；当前权威仍为 CONSTITUTION、`_HANDOFF`、backlog、域简报和施工卡。不得把 OpenSpec 旧任务勾选状态当成当前真实进度，也不得为追平勾选而伪造完成证据                             | 否           | OpenSpec 归档 / 协作流程治理；GA `INFRA-10` 总验收前                                | open | 对照已合并 PR、`_HANDOFF` 与 backlog，归档或重写该 OpenSpec 变更；提交 `openspec list --json` 不再误导当前主线、相关文档指针一致、CI / 文档门禁通过的证据                                                                                                                                                                                                                                                                                                      |
| DEFER-014 | `TenantOnboarding` 历史旧 Ant Design `Tabs.TabPane` 用法清理                                                                                              | 本轮 SVC-PILOT-01 已触碰租户开通页并迁移为 `Tabs items`，同时新增源码守卫阻断回流；不再影响租户域前端旧 API 收口                                                                                    | 否           | D2 `SVC-PILOT-01` 租户与组织服务包                                                   | done | 证据：`frontend/src/pages/tenant/TenantOnboarding.tsx` 已改为 `Tabs items`；`RulePathwayCleanliness.test.ts` 新增 `TenantOnboarding` 不含 `Tabs.TabPane` 断言；`npm test -- RulePathwayCleanliness.test.ts` 7/7 通过；`npm run verify` 44 文件 / 241 测试通过；`npm run build` 通过（保留既有 `vendor-antd` 大 chunk 提示，归 `DEFER-003`）；changed-mode 真实性 / 配置边界扫描 0 阻断；`git diff --check` 通过                                                                                                                                     |
| DEFER-015 | `PathwayTemplates` 路径配置页历史硬编码节点 / 边 JSON 和粘贴式快照试运行入口清理                                                                         | 本轮 PR2 已删除硬编码拓扑默认值，改为 L1 / L2 / L3 三层配置、结构化节点画布、L2↔L3 DSL 双向同步、真实 API-01 快照选择与 `snapshotId` 仿真；不再影响当前主线                                          | 否           | D2 `PATH-01` PR2 三层前端与页面净化                                                 | done | 证据：`frontend/src/pages/tenant/PathwayTemplates.test.tsx` 与 `RulePathwayCleanliness.test.ts` 目标测试 6/6；`npm run verify` 44 文件 / 229 测试通过；`npm run build` 通过；T-GATE 34/34、真实性全仓、配置边界、中文注释、`git diff --check` 通过；Playwright 访问 `http://127.0.0.1:5175/pathway/templates` 验证 L2→L3、L3→L2、仿真请求 `snapshotId=ctx-path-001` 且无粘贴式上下文，失败响应 0 / 非预期控制台 0，截图 `/tmp/medkernel-pathway-pr2-final-rerun.png` |
| DEFER-016 | 历史迁移全仓规约 inventory 存在旧表名、租户索引、回滚说明和国产方言历史债务（2026-06-04 `migration-convention-guard --mode=inventory` 扫描 380 个迁移文件暴露存量问题） | 当前 SVC-CLINICAL-02 PR1 未新增迁移，迁移文件级门禁 `node scripts/migration-convention-guard.mjs --mode=files $(git diff --cached --name-only)` 扫描 0 个迁移文件通过；不得宣称历史迁移规约 inventory 已清零，也不得直接改旧迁移破坏 Flyway checksum | 否           | BASE-05 迁移治理专项；GA `INFRA-10` 总验收前                                        | open | 制定不破坏已部署 checksum 的修复方案：通过新补偿迁移、经批准的基线重建或明确豁免治理历史表名/索引/回滚说明；提交 `node scripts/migration-convention-guard.mjs --mode=inventory` 无阻断或有批准豁免、H2/PostgreSQL/Oracle 迁移验证、CI 和审计记录                                                                                                                                                                                                               |
| DEFER-017 | `PathwayTemplates` 真实图形编辑缺口                                                                                                                     | 已关闭：统一接入 React Flow，节点拖拽 / 键盘移动、边创建 / 删除、节点级联删除和布局持久化均回写同一份路径表单与 L3 DSL；旧伪画布已删除，不新增兼容层或第二数据源                                                 | 否           | PATH-01 体验增强 / GA `INFRA-10` 前端体验总验收前                                   | done | 证据：模型 / 组件 / 页面测试覆盖布局持久化、连线和删除；`npm run verify` 79文件 / 528测试通过，`npm run build` 3396模块通过；路径图Playwright桌面与390px窄屏2/2通过；生产依赖审计0漏洞；T-GATE 38/38、真实性1294文件、配置边界1214文件、中文注释0 fail / 0 warn、`git diff --check`通过                                                                                                                                                                                   |
| DEFER-018 | PKG-01 知识 / 术语包级依赖适配器与离线内容迁移补齐                                                                                                     | #278 已补齐并合入：`KNOWLEDGE` 使用知识身份编码 + 权威版本号，`TERMINOLOGY` 使用术语映射包编码 + 范围键 + 包版本；不再阻塞 D2 域级验收 | 否           | D2 `PKG-01` PR1 后续硬化；D2 域级验收前必须关闭                                    | done | 证据：#278 merge `8fe8634e`；后端聚焦 / 全量通过，Docker Testcontainers PostgreSQL 15.18 / Oracle 21.3 迁移至 V53；前端目标测试 / typecheck / verify / build / 生产依赖审计通过；T-GATE 34/34、真实性全仓、配置边界、迁移 changed、中文注释、`git diff --check` 通过；项目 Playwright `/config/packages` 验证术语包选择器自动带出版本与稳定提交载荷；远端 CI 8/8 通过                                                                                                                                                                                    |
| DEFER-019 | 随访当前只有患者运行计划 / 任务，缺少可入包发布的随访模板资产模型                                                                                       | 已关闭：随访模板资产模型随 PR #600（P5 第一阶段最终收官）合并——独立随访模板资产、统一版本、五方言 V121、模板发布与计划生成、配置包选择/校验及离线导入导出均已交付；患者运行计划继续禁止作为配置资产入包 | 是           | P5 第一阶段最终收官；D3 `FOLLOW-01` 与 D2 `PKG-01` 联合关闭                       | done | PR #600 合并提交 `b410f5a3`，CI 8/8 通过；`FollowupTemplateServiceTest`、`FollowupTemplateApiContractTest`、`FollowupTemplateControllerSecurityTest`、`PackageEngineServiceTest`（随访模板发布/缺失阻断/离线导入导出）、`Followup.test.tsx`、`ConfigPackages.test.tsx` 均通过；五方言迁移 V121「followup template asset」；`mvn test` 2282 项、前端 695 项、T-GATE 全绿。详见 `docs/audit/p5-first-phase-closeout.md` §7                                                                                                                                                                                                                                  |
| DEFER-020 | 本地 Docker `medkernel-dev-backend` 容器可能与当前 worktree / 最新 `origin/main` 不一致                                                                 | 2026-06-04 SVC-CLINICAL-01 PR1 浏览器烟测发现 `localhost:18080` 由旧 `medkernel-dev-backend` 容器提供，未登录访问 `login-tenants` 返回 401；当前源码已有 `AuthControllerTest.loginTenantsIsPublic...` 证明公共契约应为 200，页面也已通过渲染与未登录重定向烟测。不得把旧容器响应写成当前源码通过或失败。 | 否           | 本地开发环境 / INFRA-10 工具链验收；后续浏览器验收前                                | open | 基于已合并的最新 `origin/main` 重新构建并启动 `medkernel-dev-backend` / dev stack，提交 `curl http://localhost:18080/medkernel/api/v1/auth/login-tenants` 200 证据、`/medkernel/actuator/health`、前端 `/login` 与受保护页重定向 Playwright 记录；若继续用旧容器，必须在验收记录中明确其版本与限制。                                                                                                                                               |
| DEFER-021 | 演练租户缺可用配置包发布适配器，术语映射包只能停在草稿态                                                                                               | 已关闭：幕4在 134 演练租户创建真实可探活 REST 发布适配器 `drill-local-runtime-package-sink-20260611`，把 `TERM.DRILL.ACT2@2026.06.11-act2-024101` 从 `DRAFT` 发布为 `ACTIVE`；发布后覆盖分析显示 `2823-3=COVERED`、`718-7=UNMAPPED`。本幕仅有一个同编码包版本，没有可作为目标的 `OFFLINE` 历史版本，因此如实登记无法执行回滚；多版本回滚体验归幕8配置包中心继续演练。 | 否           | 幕4 规则配置与模拟；幕8 继续生产级多目标发布 / 多版本回滚体验                       | done | 证据：`docs/release/evidence/v1.0-drill-20260611/幕4-规则配置与模拟/02-act2-package-release.json` 记录适配器创建、health check、发布失败修正、最终 `ACTIVE` 和 sink 同步日志；`07-coverage-after-act2-release.json` 记录覆盖分析 `2823-3=COVERED`；`README.md` 登记无 `OFFLINE` 历史版本不能真实回滚的原因。                                                                                                                                 |
| DEFER-022 | API-03 知识版本暂不能把多个来源版本的锚点合并为同一发布版本证据链                                                                                       | 幕3已验证主来源可追溯；但跨来源引用被真实拒绝，当前知识版本只能引用其绑定来源版本下的片段。不得把 CAP、法规、院内制度三类来源写成同一版本已完整合并证据链。 | 否           | 幕8 配置包中心 / GA 来源追溯增强；若幕4规则要求多来源强绑定则先做专项关闭            | open | 明确多来源证据模型：允许同一知识版本绑定多个来源版本，或新增“背景来源/主来源”分层引用；补五方言迁移、发布/追溯/退役服务测试、跨来源合法/非法组合测试、前端来源追溯展示和 134 演练证据，证明多来源链路可完整追溯且不混淆主来源。                                                                                                                                                                     |
| DEFER-023 | 本机 Docker socket 不可用导致 Testcontainers PostgreSQL/Oracle 迁移烟测跳过                                                                            | 已关闭：当前 Docker/Testcontainers 环境可用，V118 已在 PostgreSQL 15.18、Oracle 21.3 与 H2 2.2 从空库迁移并验证重复迁移；P5 清库前同时完成 134 真实备份、隔离恢复和从零迁移。 | 否           | P3 演练前发布准备；P4/P5 134 清库双重演练                                           | done | `mvn -q -f medkernel-backend/pom.xml -Dtest=FlywayMultiDialectSmokeTest test` 通过，三种数据库均到 V118；`/zoesoft/medkernel/backups/p5-pre-clear-20260612-190951/evidence/pre-clear-backup.properties` 记录隔离恢复 `117|117`、178 张表、知识 `0|0|0|0|0|0`、临时恢复库清理 0；`clear-db.properties` 记录清库后 V117 从零迁移、服务与 HTTP/HTTPS readiness 通过。 |
| DEFER-024 | 全真体验沙盘场景 #2–#10 的临床阈值、动作卡文案和证据来源尚未由有资质临床负责人完成评审 | 工程侧已登记全部 15 个场景并对 9 个未评审临床规则实施强制阻断，不能伪造阈值或以示例内容上线；不影响 #1 高钾金样和 5 个外层引擎场景的工程链路验证，但阻止 #2–#10 进入可运行状态 | 否           | 临床内容治理；正式启用对应沙盘场景前                                                | open | 临床负责人逐场景签署阈值、适用范围、风险等级、动作建议、来源证据和医师确认要求；更新 `scripts/sandbox/scenario-rules.json` 为 `APPROVED_FOR_SANDBOX`，每条至少 4 个正反测试用例；真实走 seed 治理发布、逐场景编排与嵌入反馈，归档 9 条 `PASS` 证据及 reviewer 身份，不得由代码作者代签                                                                                                                                              |
| DEFER-025 | 2026-06-18 当前工作机 Docker socket 不可用，V152 的 Testcontainers PostgreSQL / Oracle 与首发空 PostgreSQL 测试按 assumption 显式跳过 | PostgreSQL 项已关闭：冻结候选 JAR 已在 134 独立空 PostgreSQL 库两次启动至 readiness 200，从 0 表迁移到 V152 / 207 张表 / 152 条成功迁移，重复启动迁移清单哈希一致，临时库与制品已删除。首次隔离尝试因 EnvironmentFile 覆盖临时目标而误将正式库前向迁移到 V152，该偏差、纠正控制及无数据删除/无制品替换事实已完整留证。Oracle 项仍不得宣称通过，按正式支持范围在可用环境补证。 | 否 | PostgreSQL 已由 Task 6 现场预检关闭；GA 国产/多方言验收补 Oracle 实跑 | open | PostgreSQL 证据：`docs/release/evidence/p9-final-golive-20260618/pre-clear/05-postgres-empty-database-smoke.json`。Oracle：在 Oracle 21c 可用环境运行 `FlywayMultiDialectSmokeTest`，提交 0 failure / 0 skip 的 Surefire 与迁移版本证据。 |
| DEFER-026 | 134 公网 HTTPS 当前使用自签 IP 证书，且证书没有 Subject Alternative Name | 服务端 nginx、内部 HTTP 与受控固定证书探针均健康，但普通 Chromium 访问 `https://193.112.107.134/login` 返回 `ERR_CERT_AUTHORITY_INVALID`；不能把 `curl -k`、固定证书脚本或手工忽略警告写成公网浏览器信任链通过。该问题不阻塞 PR、精确制品前向部署和失败关闭门禁准备，但阻塞公网浏览器正式验收及依赖正常信任链的专家操作 | 是（阻塞浏览器正式验收） | P9 生产中心 TLS / GA `INFRA-10` 正式上线验收 | open | 提供正式域名或可签发 IP 证书的受信 CA，部署含正确 SAN 的完整证书链；以无 `-k` 的 `curl`、OpenSSL 链验证、Chromium `/login` 与登录后关键页面烟测全部通过为关闭证据，并记录证书到期监控和轮换演练 |
| DEFER-027 | 模型 Provider、模型版本与凭据引用缺少完整可视化维护入口；当前 `/system/providers` 只展示运行依赖状态，未接 `/api/v1/model-providers` 的配置、探活、启停治理 | 不阻塞当前 134：两个 Provider 已通过受控 API 配置并保持脱敏，外部模型密钥只由服务器环境变量 `MEDKERNEL_MODEL_MIMO_V25_API_KEY` 注入，数据库仅保存 `credential_ref`。但后续运维仍需调用 API 或修改受控环境文件，不能把现有状态页宣称为完整模型配置中心，也不得展示、回显或记录密钥明文 | 否 | LLM-08 Provider 治理体验完善 / GA `INFRA-10` 运维可用性验收 | open | 增加仅 `llm.provider.manage` 可见的 Provider 列表、创建/编辑、健康检查和高危启停页面；模型版本与能力策略关联可审计；凭据只维护密钥库/环境变量引用及“已配置”状态，永不回显明文。提交前后端契约与权限测试、真实浏览器六态/高危确认验收、审计事件以及网络/日志/数据库无密钥泄漏扫描证据 |

## 新问题登记模板

| ID        | 问题                                     | 当前影响                                         | 当前是否阻塞                                  | 处理阶段          | 状态 | 关闭证据                                           |
| --------- | ---------------------------------------- | ------------------------------------------------ | --------------------------------------------- | ----------------- | ---- | -------------------------------------------------- |
| DEFER-XXX | 用一句话写清不可由当前阶段真实解决的问题 | 写清为什么不影响当前主线，以及哪些红线仍不能延期 | 否 / 是。填“是”时必须停止领取下一阶段并先处理 | 写清归属卡 / 阶段 | open | 写清未来必须提交的可验证证据，不能写“人工确认即可” |
