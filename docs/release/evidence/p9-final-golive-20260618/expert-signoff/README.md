# 真人独立医学专家签署清单

运行 `3`、`4` 已在修复前发布指纹 `b1eb6de3b264ade02459e512465ef947de2ab75f` 上由 `quality-governor` 账号签为 `PASSED`；签署事件保留为历史审计，签署人真实身份与岗位由后续专家统一核查。该制品随后在真实 WHO PDF 小样本中暴露重复章节锚点冲突，Provider 与 P6 已失败关闭，因此这两条签署不得复用于修复后的新发布指纹。

锚点修复制品 `7573284bf1348fb8f437107e4c67ed626d2f3424` 已部署并完成 WHO PDF 重跑；后续 B0 候选生成暴露影子门禁定义错位，当前阶段为 `B0_SHADOW_FIX_PENDING_DEPLOY`。本目录继续约束修复后制品的真人操作边界，不得由自动化填写或冒用专家身份和意见。Codex 可提供明确标识为自动化主体的技术审查与工程放行记录，但不能替代真人医学签署。

## 签署前核验

1. 使用真实独立医学专家账号；账号须绑定真实姓名和岗位，具内置角色码 `quality-governor`（Spring authority=`ROLE_QUALITY_GOVERNOR`）及 `llm.eval.manage` 权限，并完成 MFA。
2. 专家不得是评测执行人 `platform-owner`，也不得复用来源登记或审批账号。
3. 在已建立可信 TLS 访问条件的治理页面打开运行 `3` 和 `4`；不得直接调用数据库改状态。当前公网自签证书仍受 `DEFER-026` 约束，不得绕过证书错误冒充浏览器验收。
4. 当前两个 Provider 必须仍为 `HEALTHY` 且停用，P6 必须仍为 `false`。

## 逐例核验对象

- 修复后必须为新发布指纹重新创建评测运行，运行编号以现场返回为准；旧运行 `3`、`4` 只作历史审计。
- 两个运行均绑定用例 `1`、版本 `who-chb-2024-v1`、来源 `WHO IRIS 10665/376353`。
- 两个运行的 `releaseFingerprint` 必须精确等于修复后 134 的新部署提交；运行 `1`、`2` 的空指纹及运行 `3`、`4` 的旧指纹签署均不得复用。
- 专家须逐例查看完整输入、期望短语、模型原始输出、精确来源引用、红线结果、失败原因、模型版本、发布指纹和当前基准状态，不能只看汇总的 1/1。

## 签署与拒绝

- 只有全部逐例认可时，专家才可勾选证据确认、填写不少于 10 字的真实复核意见并分别签署两个运行。
- 任一医学内容、来源、模型版本或证据不可接受时，不得签署；保持 Provider 停用和 P6=false，记录问题后返回 Task 8 重建基准或重跑评测。
- 签署完成后先核验签署人、执行人、时间、意见、MFA 事实、基准指纹、模型版本和逐例证据关联；在审计证据完整前不得进入 Provider 启用。

签署证据后续写入本目录，证据不得包含密码、Cookie、MFA secret、恢复码、患者数据或模型凭据。
