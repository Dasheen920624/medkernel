# MedKernel 引擎核心 · 从零重启总设计（唯一锚点 · v3）

> 日期：2026-06-21 ｜ 状态：设计已与用户逐段确认 ｜ 取代：[v2 引擎核心重定位](2026-06-20-medkernel-engine-core-repositioning-design.md)（已废，删除）
>
> **为什么重写 v2**：v2 方向对，但是一份 18 节的"原则文档"，自己就显得复杂、落不了地。本文按用户最终定调收敛为**一份实现者照着能直接开工的精简重启方案**，并补两条 v2 没说清的：① 决策为**全部从零重启（全新代码库 A）、借鉴现有实现**；② 知识库**平台主源 + 机构覆盖两层**。

---

## 0. 一页读懂（给实现者）

**这次重启不是新发明，是把一个已经规范完整、却被实现做爆、上不了线的系统，用狠的减法重写一遍。**

- 真实需求早就有：`FOUNDATION` 定了"先引擎全能力、后业务包装 + 十大引擎"；`业务场景 spec` 的 **S3 AI 知识工厂**主流程就是"探索→来源发现→存证→解析→新旧识别→候选→校验→审核→入包"；**S0–S40 已铺满全领域**（辅助诊疗/质控/随访/护理/医技报告/床旁/中医药…）。
- 病根是**过度实现**：95 控制器 / 795 迁移 / 15 角色 / 九闸+双签+逐条审 / 五处看知识页 / 临床 SaaS 终端页 / MFA·bootstrap 复杂登录 —— 复杂到无法正常上线。
- 重启动作：① S3 的"候选→人工逐条审"改成"候选→**全自动闸**→直接 ACTIVE"；② 人工只剩"上线时一个**旁路审批**，不卡管道"；③ 留十大引擎当地基（借鉴移植），全领域知识靠 S0–S40 覆盖矩阵当"内容"逐格灌，**绝不为每个场景另起引擎/页面/状态机**。

**权威关系**：本文一经确认即为重构顶层目标，要求同步修订 [CONSTITUTION](../../CONSTITUTION.md) §0/§2/§6/§8/§15（见 §13）。与旧卡/旧文档冲突，以"本文 + 修订后宪法"为准。

---

## 1. 产品定位

**MedKernel 医疗引擎核心**：一条自动管道从公域网 + 权威医疗网探索，生成覆盖**全领域**的整套医疗知识；再用这套知识以**核心服务能力**，经 **API + 页面嵌入**两种模式供给业务系统（HIS/EMR/CDSS）。平台自带"引擎能力控制台"，用同一套 API/嵌入在平台内演示与验证。

- 引擎是产品**主体**。"医院临床终端直接登录用"那层**降为消费/演示场景**（经 API/嵌入消费，不在本系统登录做临床工作）。
- 集团化 / 平台主权 / 多客户 / 内外网用**组织范围 + 环境维**承载，不堆角色。
- 中文产品名本轮统一用"**医疗引擎核心**"（英文/代码名仍为 `MedKernel`）。

---

## 2. 不变量（永不简化 · 都不是拖慢迭代的人工闸）

| # | 不变量 | 实现含义 |
|---|---|---|
| R1 | AI 内容明显标识 | 产出带 `aiGenerated=true` + "AI 生成"文案。免费元数据 |
| R2 | 来源 100% 可溯 | 每条知识可溯 类型/标题/版本/引用位置/hash，证据分级 A–E。免费元数据 |
| R3 | 关系库权威 + 图投影 | 业务事实唯一权威源是关系库；图库只是可重建投影 |
| R4 | 同域唯一 ACTIVE + 原子替换 | 同一适用域同时只有一个 `ACTIVE`；新版生效原子失效旧版，旧版仅历史重放 |
| R5 | **平台主版本权威 + 机构覆盖** | `t-1` 平台主版本是唯一权威核心基底；机构**不派生独立副本**，只叠**就近覆盖层(overlay)**沿组织层级回落到主版本，**禁反写/污染主源**（见 §4） |
| R6 | B0 先于模型 | 关掉模型，主链路用 B0 确定性仍跑通；模型只产候选 |
| R7 | 诚实降级 · 不伪造 | 缺模型/连接返真实态（`B0`/`NOT_CONNECTED`/`MODEL_DISABLED`），绝不假闭环、不造 hash/调用结果/随机数 |
| R8 | 运行期纯辅助 | 引擎默认不打扰、不替业务系统自动下医嘱/写病历/上报/支付；命中规则按 5 档梯度渲染（§7），无强制双签，零业务耦合 |
| R9 | 上线前一次旁路审批 | 真实上线（院内部署/外网租户启用）前，专家对要上线的发布包**点一次批准**；**不阻塞生产/探索/生成管道**（见 §6）。这是**唯一**人工医学介入 |
| R10 | 真实性门禁 T-GATE | 前端 no-page-mock + stylelint；后端禁造数/吞错/UUID 充哈希/占位 Javadoc；前后端全绿才算完成 |
| R11 | 国情合规 | 等保 2.0 三级 + 国密 SM2/3/4 + 个保法；敏感字段脱敏 + 字段级加密 + 审计链。**本轮国产化只管：国产数据库 + 国产服务器兼容**（不做浏览器内核仿真等） |

---

## 3. 系统结构（6 件 · 各管一件事）

| 件 | 干什么 | 对外接口 | 依赖 |
|---|---|---|---|
| **① 自动知识生产线** | 探索→抓取存证→解析锚定→新旧识别→候选→**自动闸**→ACTIVE，全程不停 | 提交领域格子 / 看流水线 | 领域覆盖矩阵、模型网关 |
| **② 知识库** | 知识资产信封；**平台主源 + 机构覆盖两层**；同域唯一 ACTIVE 原子替换 | 读写知识、查版本、解析有效版本 | 关系库（权威）+ 图（投影） |
| **③ 核心服务能力** | 用知识供能：查阅/字典/规则拦截/路径/推荐CDSS/质控/随访/包分发/来源追溯/嵌入 | API + 页面嵌入，统一输出契约，运行期纯辅助 | ② |
| **④ 上线旁路审批** | 知识照常自动 ACTIVE；发布包推真实上线时专家点批准 | 审批队列、`expertReviewed` 门 | 异步，**不阻塞 ①②③** |
| **⑤ 控制台/演示台** | 同一套 API/嵌入演示验证全部能力 | 4 入口：工作台·知识生产·引擎能力·系统治理 | ③ |
| **⑥ 极简底座** | 账户（密码+SSO）、少量角色、组织范围+环境、国产 DB/服务器、配置中心、审计 | 登录、租户、配置 | — |

---

## 4. 知识模型（唯一受治理实体 · 单一主版本 + 多层级机构覆盖）

全系统只有一种受治理实体：**知识资产**（信封：**身份 + 版本 + 来源 + 状态 + 适用域**）。术语/说明书/规则/路径/计算器/字典项/专病包…都是它的资产类型，共用同一信封。

**唯一核心 = 平台主版本（主源 `t-1`）**：所有知识都以平台主版本为**唯一权威基底**，由自动知识生产线产出。**机构绝不派生独立副本**（多层级机构各搞一套会知识爆炸、主版本失核心），只在主版本上叠**覆盖层（overlay）**——一份差量，不是整套复制。

**多层级机构覆盖**（集团 → 医院 → 院区 → 科室，各级都可设自己的覆盖层）：

- **运行时解析 = 就近覆盖叠加到主版本**：`科室 → 院区 → 医院 → 集团 → 平台主版本(基底兜底)`，就近的覆盖优先、逐级回落，**最终一定落到平台主版本**；某级无覆盖就用上一级/主版本。
- 机构覆盖只增量改写本组织范围、本机构生效；**不反写、不污染平台主版本，不作他方上游**（R5）；机构新增资产只归本机构。
- 同域唯一 ACTIVE（R4）= "主版本 + 生效覆盖叠加后的**有效版本唯一**"。
- 可复现：`来源 + 生产配置版本 → 同一内容 hash`（支撑 R2 + R10）。

**状态机（一套 · 生产全自动）**：
```
草稿 → 候选 →[自动质量闸]→ ACTIVE(可查阅/运行/演示) → 已下线 →(已归档)
                                  ╳ 推真实上线前：发布包级旁路审批放行
```
草稿/候选=知识生产；过自动闸→系统自动 ACTIVE；被新版原子替换→已下线（仅历史重放）。**无逐条人工审、无双签、无九闸。**

---

## 5. 自动知识生产线（全自动 · 一刻不停）

`探索 → 抓取存证 → 解析锚定 → 知识身份/新旧识别 → 候选(B0/模型) → 自动质量闸 → 入库 ACTIVE`

- **探索边界**：公域网自主获取只在**平台外网侧**（已审批白名单 / HTTPS / 许可 / robots / 拒私网 / 大小上限）；内网机构不爬外网，靠 §7 包分发拿平台主源。
- **B0 先于模型**（R6）：人工编著/权威来源结构化的 B0 即正式知识；模型做增量产候选，候选同样过自动闸；模型产出带 AI 标识（R1）。
- **自动质量闸（确定性 · 取代旧九闸/逐条审/双签）**：`schema 校验 + 红线结构完整 + 来源齐全可溯 + 重复检测 + 可复现 hash + 引用真实性`（引用真实性=机器校验候选引文确实出自所引来源对应位置，借现有医学回归/假引用判别逻辑，**不引人工**）。全过即自动 ACTIVE；**未过留在"候选"附失败原因、可修复重提，不自动 ACTIVE、不静默丢弃**（R7/R10）。
- **启用 Provider** = 纯技术就绪判定（健康检查 + 技术质量评测），全自动，不需要专家。

---

## 6. 上线旁路审批（唯一人工点 · 不卡运行）

- 生产、探索、生成、自动入 ACTIVE、演示台可用——**全程不等人**。
- 真实上线 = **院内部署** 或 **外网租户正式启用**（开始面向真实患者）。在此之前一律视为开发/演示/集成期，无需专家。
- 真实上线前：专家在审批队列对"该客户要上线的发布包"**点一次批准**，通过则发布包标 `expertReviewed=true` 方可 go-live。**审批挂在管道旁，不挡在管道中间**；专家没批，知识在平台里照样活着、可演示。
- 平台主源基础包 → 平台侧专家审；机构覆盖/启用 → 机构侧专家审；各自留痕。

---

## 7. 核心服务能力（十大引擎收口为对外能力）

| 能力 | 现有引擎 | 输入 → 输出 |
|---|---|---|
| 知识查阅 | knowledge | 医学对象 → 权威说明 + 来源 + 标识 |
| 字典映射 | terminology | 院内写法 → 标准编码 + 置信（高危近似强制 HIGH，禁字符 LCS） |
| 规则拦截 | rule / cdss | 医嘱/事件 + 上下文 → 风险提醒（5 档）+ 依据 |
| 专病路径 | pathway | 已确诊病种 + 状态 → 标准步骤 + 关键时点 |
| 推荐/辅助诊疗 | recommendation / diagnosis | 症状体征检验 → 鉴别诊断候选 + 检查/治疗推荐 + 来源 |
| 病历质控 | evaluation / quality | 病历 → 缺陷清单 + 依据 + 整改 + 评分 |
| 随访 | followup | 出院/慢病患者 → 随访计划 + 到期提醒 |
| 报告解读 | recommendation / cdss | 检验/影像报告 → 异常项 + 意义 + 建议 |
| 来源追溯 | knowledge provenance | 一条知识/结论 → 出处链（类型/标题/版本/位置/hash/分级） |
| 包分发 | pkg | 一批已发布知识 + 目标 → 版本化包，发平台/客户、内/外网 |

**统一约束**：所有输出带 `status` + `source`（R2）+ `aiGenerated`（R1）。两种交付：**API**（业务系统自渲染）/ **页面嵌入**（iframe·Web 组件·SMART on FHIR·CDS Hooks 卡片，业务系统不重做界面）。

**运行期打扰梯度（R8 · 逐规则可配）**：

| 档 | 代码 | 行为 | 阻断 |
|---|---|---|---|
| L0 | SILENT | 静默旁路，只记录/角标 | 否 |
| L1 | INFO | 弹窗/卡片告知，可直接关 | 否 |
| L2 | ACK | 必须点"已知悉"才继续 | 否 |
| L3 | REASON | 填理由放行，理由留痕 | 软 |
| L4 | HARD_STOP | 硬停，业务系统按合同阻断该医嘱 | 硬 |

默认不打扰；到哪档由**规则配置**决定，引擎不统一强加；无强制双签。

---

## 8. 全领域覆盖（S0–S40 当"内容"，不当开发泳道）

- "全领域" = **领域覆盖矩阵 × 同一条管道**：S0–S40 / 全医疗专业领域矩阵当**待覆盖清单**，引擎按格子自动探索-生成，直到覆盖；工作台看"已覆盖 X / 全 Y"。
- 加新领域 = 往矩阵加格子，**复用同一管道 + 同一批引擎**；**封顶不开第二套引擎/页面/状态机**（防再次膨胀的硬约束）。
- 业务服务包（试点/临床运行/质控/合规/第三方/专病/专业协同）= 引擎能力的**组合包装**，不另起实现——但本轮重启**先把引擎主干 + 自动生产线跑通**，业务包装后置。

---

## 9. 角色与登录（极简）

- **登录**：账户密码 + **SSO 单点登录**。删 MFA 强制、bootstrap token 等复杂流（内置超管仍独立高亮审计）。
- **角色 = 权限预设包 × 范围（平台/机构）× 环境（内/外网）**，5 预设：

| 预设 | 职责 |
|---|---|
| 内置超管 | 系统兜底，强制内置、不可降权/旁路、独立审计 |
| 运营管理 | 机构/账号/接入/Provider/配置/运行保障/国产化 |
| 知识生产 | 跑探索、跑生产、编著候选（全自动入库，无需人工审） |
| 医学专家 | **仅真实上线前**做一次旁路审批（R9） |
| 合规审计 | 只读查审计与证据 |

被移除的登录角色（临床决策/护理/医技/临床治理/质量医保运行侧…）→ 改为业务系统经 API/嵌入消费。

---

## 10. 底座与国产化

- **关系库权威 + 图投影**（R3）：核心数据在关系库；图库（Neo4j）只做关系/路径查询投影，可重建。
- **5 方言迁移重置 V1 干净基线**：h2/postgres/oracle/dm/kingbase 一致 + 中文 COMMENT + 索引约束 + 组织/版本/审计字段；不背旧版本号、不做兼容回填。
- **国产化（本轮只两项）**：国产数据库（达梦/人大金仓）+ 国产服务器/OS（麒麟/统信/openEuler）兼容；国密 SM2/3/4 smoke。其余国产化项不在本轮口径。
- **配置外置**：业务/运营配置走配置中心，可审计、高危项有护栏；除启动必需外不写死 yml。

---

## 11. 借鉴 / 重建 / 舍弃（决策 A：全新代码库、借鉴现有）

> 全新代码库 + 干净结构；现有代码只作**借鉴素材**（移植已验证逻辑，不搬旧结构/旧债）。

| 处置 | 对象 |
|---|---|
| **借鉴移植**（把逻辑搬进新库，去旧壳） | 十大引擎：knowledge 资产模型 · terminology（语义映射+高危近似）· rule+DSL · pathway · recommendation/cdss · evaluation/quality · followup · pkg · embed · llm 模型网关 · acquisition 获取 · 来源追溯 · projection 图投影 · datascope · audit · crypto 国密 |
| **干净重建** | 知识状态机（简化为 §4 一套）· 角色权限（15→5 预设）· 登录（密码+SSO）· 迁移 V1 基线 · IA（4 一级入口）· 工作台 · 自动质量闸（取代九闸） |
| **舍弃**（不进新库） | 九闸/双签/逐条审/医学激活闸 · release-freeze/provider 签字证据/兼容迁移/`credential_ref` · 临床 SaaS 终端页（患者索引/提醒/待办/驾驶舱…）· 五处看知识页 · MFA/bootstrap 复杂登录 · 浏览器内核仿真国产化 · 15 角色矩阵 · 历史死测试/计划/总结/审计副本 |

---

## 12. 施工路线（一格一格 · 先证脊活着）

| 片 | 目标 | 验收（除 §14 通用外） |
|---|---|---|
| **S0 地基** | 新库骨架 + 关系库 V1 基线 + 密码/SSO 登录 + 5 预设 × 范围 × 环境 + 4 入口空壳 + T-GATE CI | 构建/测试绿、超管不可旁路、范围/环境隔离绿、无旧角色/旧闸 |
| **S1 止血** | 一格领域走完 `探索→候选→自动闸→ACTIVE`，工作台 + 演示台看见 | 知识数 ≥1；演示台查得到；全自动、无伪造 |
| **S2 知识库两层** | 资产信封 + 平台主源/机构覆盖 + 图投影 + 来源追溯 | 同域唯一 ACTIVE 原子替换；机构覆盖优先、不污染主源 |
| **S3 服务能力** | 先查阅 + 字典 + 规则拦截，API+嵌入，运行期纯辅助 + 5 档 | 各有真实 E2E；默认不打扰；降级诚实 |
| **S4 旁路审批** | 异步发布包审批，`expertReviewed` 门控 go-live | 审批不阻塞 ①②③；未批知识仍可演示 |
| **S5 全领域铺开** | 领域覆盖矩阵 + 覆盖率驱动批量自动生成；其余能力（路径/质控/随访/报告解读…）按内容挂上 | 覆盖率可视；新格子复用同管道、不另起引擎 |

> S0 先清场（=新库从零，不在旧库上改）；S1 证整条脊活着。每片基于最新 `main`，一逻辑单元一 PR，先失败测试→实现→绿。

---

## 13. 拟修订的宪法条款（同 PR）

- **§0 定位/命名**：集团医疗智能中枢 → **医疗引擎核心**；临床中枢/七业务域降为消费/演示场景。
- **§2 菜单 IA**：七业务域 → 4 一级入口（工作台/知识生产/引擎能力/系统治理）。
- **§6 临床医疗安全**：逐条审/高危双签/医师确认强耦合 → **单旁路审批（上线前一次专家批准）+ 运行期纯辅助 + 配置化 5 档打扰**；保留 AI 标识、来源可溯、引擎不自动写、不伪造。
- **§8/§11 生产闸**：九项生产闸/模型签字门 → **全自动技术质量闸**；模型只产候选自动入库；上线前一次旁路审批。
- **§15 角色矩阵**：15 角色 → 5 预设 × 范围 × 环境；登录简化为密码+SSO。
- **不动**：AI 标识、来源可溯、关系库权威、唯一权威知识、平台主源不污染+机构覆盖、诚实降级与真实性门禁、国情合规（即本文 R1–R11 全部保留；本轮只改闸门/角色/登录/定位/IA）。

---

## 14. 验收 / 真实性门禁（每片必过）

- T-GATE：前端 no-page-mock + stylelint；后端禁造数/吞错/UUID 充哈希/占位 Javadoc；前后端全绿。
- 六态完整、服务端分页、5 方言迁移一致、中文优先、文档随代码同 PR。
- R1–R11 全程不破；演示与生产不造数；降级诚实。
- 每片更新 [_HANDOFF](../../_HANDOFF.md) 状态/下一步/证据。

---

## 15. 一句话本质（贴墙上防跑偏）

> MedKernel 是**医疗引擎核心**：一条**全自动**管道从公域网+权威医疗网探索，生成覆盖全领域的医疗知识（**单一平台主版本权威 + 多层级机构覆盖 overlay、不派生独立副本**），用十大引擎能力经 **API + 页面嵌入**供业务系统，并用同一套 API/嵌入在演示台演示。生产全自动（探索→候选→自动闸→ACTIVE），**唯一人工是真实上线前一次旁路审批、不卡管道**；运行期纯辅助、默认不打扰、命中规则 5 档渲染。角色靠`权限×范围×环境`（5 预设），登录只密码+SSO。**全新代码库、借鉴现有、留十大引擎当地基。**
