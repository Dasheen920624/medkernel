# 会话接力

> 开工先读本文件。只保留当前执行需要的事实；历史过程查 Git。

## 当前主线

- 原始代码基线：PR #647 合并提交 `acd511c0`；
- 工作分支：`codex/647-launch-simplification`；
- worktree：`/Users/zhikunzheng/.config/superpowers/worktrees/codex3/647-launch-simplification`；
- 不读取、比较或继承 #648、#649 的方案；
- 完成本地实现和验证前不推送；
- 最终目标是完整上线，不是首发裁剪。

当前权威：

- [完整上线设计](superpowers/specs/2026-06-21-647-launch-core-contraction-design.md)
- [实施计划](superpowers/plans/2026-06-21-647-launch-core-contraction.md)
- [功能审计](audit/647-launch-functional-audit.md)
- [产品宪法](CONSTITUTION.md)

## 已确认事实

- 前端 44 条路由、54 个页面组件；
- 后端 93 个 Controller 文件、95 个 `@RestController`、501 个 `@PreAuthorize`；
- 34 个菜单、115 个权限编码；
- 五方言各 159 个迁移，最终 214 张表；
- 后端基线 3131 tests 通过，前端 830 tests 通过，CLI 30、MCP 16 通过；
- 本地提交 `adc5a33d` 的四旧角色只能覆盖 25 个菜单，遗漏 9 个临床/质量/通知入口，不能作为最终方案；
- 当前 MFA 只检查绑定，没有在登录时验证 TOTP；
- 当前知识、规则、模型、来源、发布和导出流程存在多处双人、专家或委员会门槛；
- 模型自动知识所需 Provider、评测、来源、解析、生成、安全门、影子和发布能力已经存在，主要阻断是人员门；
- 一份裸 SQL 不能兼容五库，采用一份规范化模式模型生成五份 V1。

## 正确方向

1. 保留全部真实页面和接口；
2. 使用医疗引擎、知识生产、平台管理三个产品空间重组现有八业务域；
3. 重建为平台管理员、医疗引擎运营员、临床使用者、审计员四个可分配职责，超管内置；
4. 删除旧角色兼容、租户权限覆盖、前端角色门和服务层旧角色判断；
5. 删除双签、委员会、独立专家和职责分离，保留技术校验、责任确认、审计、影子、灰度和回滚；
6. MFA 默认关闭；开启后登录真实验证 TOTP；
7. 技术评测通过即 `PASSED`，删除独立专家签字和 P6；
8. 贯通真实大模型自动生成小批权威知识；
9. 压缩迁移为规范化模式模型和五方言 V1；
10. 清除旧代码、旧方案、旧截图和历史证据。

## 134 当前外部状态

- 当前运行旧制品提交 `2c502f1e547a185dc5ab95a76d7a3329c4d1f724`；
- 当前 Flyway 为 V159；
- Provider 已登记但停用；
- 当前没有正式模型知识激活；
- 文献资料根为 `file:///medkernel-data/platform-knowledge/t-1/literature-materials/`；
- 浏览器证书仍是无 SAN 的自签证书，公网浏览器验收不可宣称通过；
- 本轮本地完成后，按用户授权先备份，再清库、部署 V1、生成知识并做全流程演练。

## 当前进度

- 完成：全量页面、接口、权限、MFA、人员门、模型门、迁移和文档证据核查；
- 完成：原位重写错误收缩设计、实施计划和功能审计；
- 进行中：按 TDD 重建完整功能覆盖和最简职责权限；
- 未开始：代码实现、迁移 V1、文档批量清理、134 重部署、远程 PR。

## 下一步

1. 写失败测试锁定四个新职责和 34 菜单全覆盖；
2. 删除权限覆盖和前端 `requiredRoles`；
3. 增加三产品空间并保持全部路由；
4. 按实施计划继续本地提交。
