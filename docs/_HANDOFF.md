# 会话接力交接（_HANDOFF）

开工只读本文件续接；历史细节查 git / PR / 任务卡，不在这里堆施工史。

## 当前唯一主线

- 分支：`codex/unify-mainline-stabilization`
- 基线：`origin/main` = `b85542ee`
- 线1已统一接管线2 / 线3 / 线4；禁止再开独立施工线或重复实现。
- 项目未上线：删除无价值兼容分支，保持唯一模型、唯一接口、唯一主流程。
- 当前阶段：已合入和已标 `done` 能力完成统一验收基线，继续推进线1剩余任务。

## 当前统一基线

- 登录、会话、平台租户、首次改密与 MFA 接管已统一；Vite 代理不再硬编码端口。
- 规则、路径、字段目录、术语、配置包统一使用共享条件模型、版本资产和发布状态机；ACTIVE 快照不可变。
- 患者路径、随访、CDSS、质控、医保、诊断知识、MPI、通知和协作任务均接入真实后端主链路。
- 组织、用户、角色、身份绑定、数据权限和整改指派统一使用服务端目录与分页，不再浏览器伪造。
- FHIR / HTTP / Webhook / WebService、适配器健康、重试死信和包同步均走真实连接器；未连接时诚实返回 `NOT_CONNECTED`。
- 运行证据、备份演练、国产化检查和审计页面只展示后端事实，不在页面伪造执行结果。

## 当前证据

- 后端：`mvn -q test`，1769 个测试通过；96 条迁移完成五方言验证。
- 前端：76 个测试文件、505 个测试通过；ESLint、Stylelint、规则测试、Prettier、类型检查和生产构建通过。
- T-GATE：真实性、配置边界、迁移规约、中文注释和 `git diff --check` 通过。
- 浏览器：桌面和 390px 移动端登录、工作台、审计、运行状态无页面级横向溢出或控制台告警；真实登录、首次安全设置、审计详情 / 导出审批、依赖探测与专家诊断可操作。
- 当前服务：前端 `http://127.0.0.1:5174/`；后端 `http://localhost:18082/medkernel`，健康状态 `UP`。

## 下一步

1. 继续线1最新未完成任务；每次只做一个完整业务闭环。
2. 新能力必须复用当前目录、权限、分页、状态机、上下文快照和真实连接器，不得另起兼容层。
3. 每项完成后更新代码、测试和本文件中的一行当前状态，不新增施工说明文档。

## 最小完成口径

一项能力重新标 `done` 前，至少满足：页面能人工操作、后端走真实接口、无页面假数据、失败诚实展示、权限与租户不绕过、测试 / T-GATE 有证据。
