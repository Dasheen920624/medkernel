# 会话接力

## 当前执行

- 分支：`codex/evidence-governance-57`；基线：`origin/main` = `ae26b8c6`（PR #507 / 5.6 已合入）。
- 线1统一承接全部任务；线2 / 线3 / 线4不再独立开发或维护重复实现。
- 项目未上线：保留唯一模型、唯一接口、唯一主流程，不做旧方案兼容层。

## 5.7 状态

- 已建立平台/租户资产身份编码、循证等级、复审周期和服务端分页复审队列；知识可先进入迁移宽限期，再由配置驱动任务退役。
- 已统一弃用后继、迁移说明与溯源展示，平台管理员可进入知识治理；空数据时筛选仍可操作，工作台生命周期步骤在窄屏不再重叠。
- 已补 V109 五方言迁移及存量 ACTIVE 复审日期回填、目标租户审计、知识台账服务端翻页、退役服务/任务、API 契约与前后端回归测试。
- 本地证据：后端 `mvn -q test`；前端 `npm run verify`（82 文件 / 586 用例）；OpenSpec strict、38 个门禁自测、真实性/配置边界/迁移/中文注释门禁和真实浏览器桌面/窄屏验收均通过。
- 待补：提交、远端 CI、合并。

## 下一步

1. 完成 5.7 PR、CI 与合并。
2. 从最新 `main` 继续 5.8：发布前影响模拟、灰度放量、批量/模板/克隆。
