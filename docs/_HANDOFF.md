# 会话接力

## 唯一执行组织

- 当前分支：`codex/d6-provenance-stabilization`
- 基线：`origin/main` = `1eb0237c`
- 线1统一承接全部任务；线2 / 线3 / 线4不再独立开发、抢合并或维护重复实现。
- 项目未上线：只保留唯一模型、唯一接口、唯一主流程，不新增兼容层。

## 当前状态

- D0–D5及线2 / 线3 / 线4在D6之前的承接项已合入主线。
- D6已完成：`OPT-10`插件安全边界、`DOMCHK-01`国产化自检、`DEVCON-01`开发者控制台。
- `PROV-01`已完成本地实现与真实浏览器验收，待本分支合入主线。
- D6未完成：`GRAPH-01`图谱查询、`AIFLOW-01`AI工作流；域级验收仍为`pending`。

## 当前证据

- 主线基线证据：后端1781项、前端509项、三类T-GATE与三方言迁移验证均通过。
- `PROV-01`前端全量通过：76个测试文件、511项测试；ESLint、Stylelint、规则测试、格式、类型检查和生产构建通过。
- `PROV-01`后端全量通过：1786项测试，0失败 / 0错误 / 0跳过；H2、PostgreSQL、Oracle均完成V1–V97迁移与幂等验证。
- T-GATE通过：38项门禁自测、真实性1284文件、配置边界1214文件、迁移规约485文件；中文注释0 fail / 0 warn。
- 真实浏览器：正式API创建并激活知识版本，`?identityId=1`直达来源、版本、锚点和指纹；1440px与390px无页面级横向溢出。

## 下一步

1. 完成`PROV-01`全量门禁、提交、PR、CI与合并。
2. 按`GRAPH-01 → AIFLOW-01`继续D6统一整改。
3. 每次只更新本文件、backlog和对应卡片，不新增施工说明文档。
