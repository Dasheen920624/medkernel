# 会话接力

## 唯一执行组织

- 当前分支：`codex/line2-rule-governance`
- 基线：`origin/main` = `7ba10cff`
- 线1统一承接全部任务；线2 / 线3 / 线4不再独立开发、抢合并或维护重复实现。
- 项目未上线：只保留唯一模型、唯一接口、唯一主流程，不新增兼容层。

## 当前状态

- D0–D6及线2 / 线3 / 线4已承接代码均已合入主线；历史done状态只代表已交付，仍需按真实使用链路重新验收。
- D6已由PR #458–#461统一整改并合入主线；AI工作流只保留真实能力目录与状态查看。
- 全量复核第一批PR #462已合入主线：建立真实权限画像全路由E2E，并修复通知设置默认配置来源非法导致的409。
- 线2路径图形编辑已由PR #463合入主线；React Flow交互与路径DSL保持单一数据源。
- 线2规则P7-1已由PR #464合入主线：动作闭集、多动作卡、来源、建议项、覆盖理由和高危医师确认统一进入规则DSL与CDS Hooks输出；旧动作形态和重复前端DSL实现已清除。
- 线2规则P7-2已完成本地验收、待PR合并：规则优先级、抑制、持久化窗口去重、发布前静态冲突检测和人工继续理由留痕已统一到现有规则主链路；规则触发只接受标准值，发布复用统一版本服务。
- 当前下一项为P7-3适用域；本分支完成全量门禁、提交、PR与合并后继续。
- 线2 / 线3 / 线4尚未全部完成；继续按原OpenSpec未勾选任务和真实页面链路逐项实现、验收。

## 当前证据

- 前端全量：78个测试文件、524项测试通过；ESLint、Stylelint、格式、类型检查和生产构建通过。
- 真实浏览器：14项登录域、2项GRAPH、2项AIFLOW E2E通过。图谱通过配置中心开启、正式API创建发布知识并重建；AI工作流真实返回8项B0能力，无执行/配置入口；桌面与390px移动端无根级横向溢出。
- 全路由E2E：13个真实角色画像自动覆盖全部受保护路由；页面主标题、权限、HTTP错误、浏览器错误和根级横向溢出检查通过。
- 路径图E2E：桌面与390px窄屏2/2通过；真实连线、删除、拖拽、DSL布局持久化、Escape不误关弹窗和根级无横向溢出通过；生产依赖审计0漏洞。
- 通知设置回归：`WorkflowNotificationSettingsServiceTest` 6项通过；真实页面不再因非法`SAFE_DEFAULT`配置来源返回409。
- 规则动作回归：前端规则模型/页面28项通过；后端新增动作结构、旧格式拒绝、空动作拒绝和高危确认测试通过；真实登录规则库完成双动作、建议项与详情交互，桌面及390px窄屏无根级横向溢出、无控制台错误。
- 规则治理真实验收：医务处可配置优先级、抑制规则和去重窗口；临床医生首次执行返回动作、窗口内再次执行显示去重并关联首次执行，人工继续强制理由且写入审计；桌面与390px页面无根级横向溢出。
- 后端全量279个测试套件、1810项通过，0失败 / 0错误 / 0跳过；H2、PostgreSQL、Oracle均迁移至V97并完成幂等复跑。
- T-GATE：26项门禁自测通过；changed-mode真实性25文件、配置边界21文件通过；inventory真实性1297文件、配置边界1217文件通过；中文注释0 fail / 0 warn，类级中文Javadoc与三方言表注释全量覆盖100%。

## 下一步

1. 完成P7-2全量门禁、PR与远程主线合并。
2. 承接P7-3适用域，并继续按OpenSpec顺序完成线2未勾选阶段。
3. 逐项承接线3 / 线4未完成任务与真实工作流验收，清除被主线完整实现取代的重复接口、兼容层和不可用交互。

文档只维护本文件、backlog、域简报和对应卡片，不新增施工说明。
