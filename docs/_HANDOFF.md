# 会话接力

## 当前执行

- 分支：`codex/package-entitlement-67`；基线：`origin/main` = `28f2c869`。
- 线1统一承接全部任务；线2 / 线3 / 线4不再独立开发或维护重复实现。
- 项目未上线：保留唯一模型、唯一接口、唯一主流程，不做旧方案兼容层。

## 6.7 状态

- 受限平台包已具备唯一授权模型；仅平台管理员可创建受限包并为启用中的客户租户开通、续期或撤销授权。
- 有效包解析、模板引用和分发均前置校验授权；未授权或已撤销不可见且不下发，到期返回明确冲突状态，平台租户不受限制。
- 配置包中心已提供真实租户选择、授权状态与审计原因；登录到创建、授权、撤销的真实浏览器链路及 `390×844` 窄屏已通过。
- 本地证据：后端 `2121` 用例通过、`2` 项 Docker 环境跳过；前端 `83` 文件 / `597` 用例、生产构建及 Playwright 端到端通过。
- 本地门禁已通过；待补：提交、远端 CI、合并。

## 下一步

1. 完成 6.7 PR、CI 与合并。
2. 从最新 `main` 继续 7.1，物理收敛旧并行表与组织树二期。
