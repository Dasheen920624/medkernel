# 会话接力

> 开工只读本文件续接；产品不变量见 [CONSTITUTION](CONSTITUTION.md)，完整范围见
> [PRODUCT_SCOPE](PRODUCT_SCOPE.md)，实施合同见当前 OpenSpec。历史过程、作废候选与旧任务证据只用 Git 追溯，
> 不在本文件保留第二套事实源。

## 立即续接

- 唯一 OpenSpec：`converge-full-launch-and-knowledge-platform`，当前 **8/23 个结果任务完成、15 个待执行**。
- 隔离工作树：`/Users/zhikunzheng/.config/superpowers/worktrees/codex3/launch-convergence`；分支：`codex/launch-ledger`。
- 当前下一项：任务 2.5，复用已经闭环的真实 `.mkp` 导出、隔离预检、空库物化和 CAS 激活能力，在现有 `ReleaseGovernance` 完成真实文件交互，并以两个数据库、目录和实例身份完全独立的本地实例证明导出—介质复制—导入—运行—升级—回滚；不得提前实现 DELTA、HTTPS 拉取或第二套发布框架。
- 受保护原工作树 `/Users/zhikunzheng/个人/郑志坤/medkernel/codex3` 含用户改动，禁止回滚、暂存、清理或污染。

## 当前路线为何是主线

- 旧任务图为 205 项、24 项完成、181 项待办、537 条直接依赖、最长关键路径 152，过度串行是推进缓慢的真实原因。
- 首轮收敛后再次核查发现仍有一条倒置依赖：完整生产医疗资源和完整院内断网交付被错误地放在 134 软件部署之前，与“软件稳定后先上 134、再生产资源、最后形成医院交付物”相反。现已进一步收敛为 23 个结果任务、28 条直接依赖、最长关键路径 17；到 134 软件部署的最长距离为 12 个结果任务，正式资源生产和院内交付不再阻塞首台平台上线。
- 首发不建设自动跨宿主交接状态机、DELTA 链、HTTPS 自动拉取、独立权威页面、第二套部署框架或第二套证据编排器。医疗责任审核和 134 最终一次破坏性确认仍是不可删除的安全边界。

## 不可替代的可信基线

- 固定来源锚点：`7217504ce82e1aa119c3402e3b5d054f9369e018`，不得直接提升。
- 已保全 RC0：`7674532fdb4f3db8373bb996369cfc1fe359c553`；run-id `rc0-20260711T132737Z-7674532fd-r10`；原始 bundle：`/Users/zhikunzheng/.medkernel-rc0-runs/rc0-20260711-7674532fd-r10/bundle`。
- RC0 的九类门禁、六类制品、复制复验、篡改拒绝和 PR #654 required CI 8/8 均真实通过；保全提交已以 `4c6795aaa6ba6481756e239fefff90afd1c724a2` 合入当时的 `origin/main`。
- RC0 只证明冻结输入可复现。其后的权威、`.mkp` 和上线实现会形成新的上线 RC；不得把旧 RC0 制品或历史证据拼接成当前完成结论。

## 已完成结果

- 1.1：保全可复现 RC0 并合入主线。
- 1.2：建立 35 入口与 `LAUNCH-01`～`LAUNCH-15` 唯一机器总账，缺证、未知、跳过或自证不能过账。
- 1.3：建立稳定平台权威、独立 issuer/key、外置 HSM/KMS 私钥边界和五方言 V1 持久化骨架。
- 1.4：固定根 SM2 签发验签、首个活动 issuer/key 和不可变 FULL 包登记闭环完成；拒绝 TOFU、宿主/IP 冒信任、吊销/过期 key、重放、同序号异摘要和 DELTA。
- 2.1：确定性 FULL manifest 与 13 类统一适配器完成。manifest 固定 UTF-8 规范字节，绑定 authority/issuer/key/发布序号/平台版本/兼容范围/逐文件 SM3，拒绝未知宿主字段、父链、绝对或非规范路径、重复/未排序文件；13 个 `VersionedAssetType` 恰好各一适配器，统一 `export/validate/materialize`，携带完整正文、多来源、许可文件摘要、精确依赖、绑定版本的校验事实和确定性合成测试证明。规则共享条件片段、路径子路径/继承及路径环复用同一自包含策略阻断。证据键：`mkp.full.manifest-adapters13`。
- 2.2：真实 FULL `.mkp` 生成、签发、登记和下载闭环完成。服务端快照经 13 类适配器装配完整正文、来源/许可、精确依赖、校验、合成测试、停用/撤回和平台版本文档；容器固定排序、无压缩和时间元数据，写临时文件后强制落盘、逐条重读并原子提交。固定根 SM2 信封、manifest SM3、整文件 SM3、真实大小和受管相对坐标不可变登记，下载从同一文件描述符复算；登记事务回滚可严格恢复同一已签文件，符号链接越界、患者直接标识、变体凭据键和私钥材料均阻断。正式关系库医疗资源快照由 3.3 接通，完整生产内容在软件部署 134 后由 5.2 生成；当前缺快照端口时 API 诚实返回不可用，不生成空壳。证据键：`mkp.full.export-download-signed`。
- 2.3：真实文件流进入内容寻址隔离区，强制原子落盘与摘要重读；严格拒绝路径逃逸、符号链接、加密/压缩条目、大小/数量/展开比越界、容器或 manifest 漂移、非固定根签名、不兼容版本、越权许可、患者实例/直接标识/凭据、13 类正文或依赖缺失。预检事实绑定整包、签发链、目标医院及当前机构版本；相同状态幂等，状态变化产生新事实，只有已物化包注册表推进防回放游标，未激活预检不会阻塞合法旧序号。证据键：`mkp.full.upload-quarantine-preflight`。
- 2.4：仅有唯一 V1、目标医院和预置信任根的空库可在单事务中重建公开签发链、平台版本、13 类稳定身份/正文/来源许可/依赖/测试、撤回事实和不可变包登记，私钥不进入应用或数据库。激活前重验同一隔离对象、签名和预览，医院行锁与期望当前版本共同实现 CAS；首次接受非连续平台发布序号，重试幂等，并发、预检后换包、中途失败和已撤回内容回滚均不改变原机构运行状态。部署合同同时持久化正式包与隔离目录。证据键：`mkp.full.blank-v1-atomic-lifecycle`。

## 当前验证事实

- 任务 2.3/2.4 按 TDD 覆盖真实 ZIP、越界压缩、签名/摘要/兼容/许可/依赖失败零业务写入、空 V1 物化、首次非连续发布序号、预检状态绑定、同序号异摘要、未激活预检不推进回放游标、CAS 并发、中途回滚和安全撤回回滚；定向测试与全量后端回归均通过。
- 后端全量 `mvn -q test`：548 个测试套件、3295 项，失败 0、错误 0、跳过 7；7 项仅为当前宿主无 Docker 的 PostgreSQL/Oracle smoke（5 项）和需显式大规模环境开关的方言容量测试（2 项），未冒充已验证。前端 `npm run verify`：116 个文件、2221 项全通过；生产构建通过。
- 五方言 V1 与 schema 生成检查 8/8；部署资产合同、35 入口目录/消费者、严格 OpenSpec 和 `git diff --check` 通过。真实性 56/56、配置边界 2/2、签名秘密 7/7、迁移规约 14/14、性能合同 4/4、中文注释 fixtures 7/7，生产源码和三种正式数据库方言中文覆盖 100%，各全量扫描零阻断。
- 本轮没有连接或修改 134，也没有把本地验证资源冒充已生产并责任审核的正式医疗资源。双实例文件往返、正式资源覆盖矩阵、真实 openEuler/PostgreSQL、容量、断网和目标稳定窗口仍由后续结果任务提供。

## 下一执行序列

1. 2.3～2.5：真实上传进入隔离区，统一预检，空 V1 库全有或全无物化，CAS 激活/回滚，并在两个独立实例间完成文件往返。
2. 3.1～3.3：只补齐覆盖矩阵、获许可来源、13 类资产生命周期、readiness 和正式包快照的软件能力；本地验证资源不得冒充生产内容。
3. 4.1～4.3：完成本地双实例软件演练，冻结同一提交的软件 RC，随后只读预检 134。
4. 5.1～5.4：在唯一确认内先部署 134，再在 134 生产审核完整资源、签发包、全系统演练并完成 48～72 小时稳定窗口。
5. 6.1～6.4：软件 RC 冻结后可并行形成院内断网交付物和真实空机 smoke；待 134 稳定后用同一软件与 `.mkp` 完成空医院复制并收尾。

## 上线与 134 边界

- 开发阶段只运行自动回归，不增加人工过程门禁；医疗内容正式发布仍须有资质责任人审核，AI 不自动开嘱。
- 软件系统、真实 `.mkp` 导入链路和双实例运行未完成本地稳定验证前不得写入 134；正式生产医疗资源包和完整院内断网交付物不再作为 134 软件部署前置。当前对 134 仍只允许已授权的只读预检。
- 清库、停机和覆盖部署只保留最终一次确认，且必须绑定主机、数据库、目录、候选提交、run-id、备份恢复点、删除范围和窗口；任何漂移使确认失效。
- 134 或未来服务器可作为平台知识管理源；医院只消费固定版本、可验签、可离线导入、可回滚的完整包，平台更新不得绕过医院审核和机构生效版本。
- 禁止真实患者数据、凭据、私钥、数据库密码、JWT 或未脱敏目标证据进入仓库、日志、`.mkp` 和普通备份。
