# 会话接力

## 当前执行

- 分支：`codex/release-simulation-58`；基线：`origin/main` = `f853b425`（PR #508 / 5.7 已合入）。
- 线1统一承接全部任务；线2 / 线3 / 线4不再独立开发或维护重复实现。
- 项目未上线：保留唯一模型、唯一接口、唯一主流程，不做旧方案兼容层。

## 5.8 状态

- 已统一发布前影响模拟、历史病例回放、安全/依赖门禁、摘要确认、灰度策略与指标观测；越阈值自动暂停并通知，可按上一发布钉点回退。
- 已统一覆盖模板、批量预演/生效/撤销与跨机构克隆；规则、路径、评估、包和术语发布语义归入同一版本治理主流程，旧灰度字段和重复入口已清除。
- 已新增发布治理页面与 V110 五方言迁移；桌面及 390×844 窄屏无横向溢出，登录、首次改密、双因素绑定、发布治理与模板弹窗均通过真实浏览器验收。
- 本地证据：后端 `mvn -q test`（2085 用例，0 失败 / 0 错误 / 2 跳过）；前端 `npm run verify`（83 文件 / 594 用例）与 `npm run build`；OpenSpec strict、真实性/配置边界/迁移/中文注释门禁均通过。
- 待补：提交、远端 CI、合并。

## 下一步

1. 完成 5.8 PR、CI 与合并。
2. 从最新 `main` 继续 6.6：第三方接入 API 契约与 OpenAPI 文档。
