# 会话接力

## 唯一执行组织

- 当前分支：`codex/line2-rule-applicability`
- 基线：`origin/main` = `2dd518e4`
- 线1统一承接全部任务；线2 / 线3 / 线4不再独立开发、抢合并或维护重复实现。
- 项目未上线：只保留唯一模型、唯一接口、唯一主流程，不新增兼容层。

## 当前状态

- D0–D6及线2 / 线3 / 线4已承接代码均已合入主线；历史done状态只代表已交付，仍需按真实使用链路重新验收。
- PR #462–#465已合入：全路由权限复核、通知设置修复、路径图形编辑、规则动作与交互治理已统一进入现有主流程。
- P7-3适用域已完成本地实现：DSL `applicability` 是唯一权威，`rule_applicability` 仅作查询镜像；创建、测试、仿真、运行、推荐和离线包共用同一判定服务。
- 适用域覆盖人群纳入/排除、集团/医院/科室、住院/门诊/急诊/随访、生效期和稳定灰度；不适用统一返回 `NOT_APPLICABLE`，不产生动作。
- 临床事件入口、持久化、FHIR门面和规则上下文统一使用四值 `clinicalSetting`，不再因缺少就诊场景把真实事件误判为不适用。
- P7-3全量、真实浏览器和T-GATE验收已完成，待提交PR并合入远程主线。
- 线2 / 线3 / 线4尚未全部完成；继续按原OpenSpec未勾选任务和真实页面链路逐项实现、验收。

## 当前证据

- 前端：`npm run verify`通过，78个测试文件、529项测试全绿；ESLint、Stylelint、格式和类型检查通过，生产构建通过。
- 后端：280个测试套件、1826项测试全绿，0失败 / 0错误 / 0跳过；五方言迁移契约通过，H2、PostgreSQL、Oracle空库迁移至V97并完成幂等复跑。
- 真实浏览器：从登录、首次改密、MFA绑定进入规则库，L3默认隐藏；L2适用域无原始JSON输入，可视化纳入条件可配置；集团/医院/科室分别按层级与关键词检索真实ACTIVE组织目录，不受首100条截断且不允许自由录入技术ID；已选组织始终保留可读名称；真实创建草稿并在详情显示四场景、组织、生效期、灰度和人群范围。
- 响应式：规则创建弹窗受视口宽度约束，桌面和390px窄屏无根级横向溢出，条件编辑无重叠；浏览器控制台0错误。
- T-GATE：38项门禁自测通过；全仓真实性1310文件、配置边界1230文件、迁移规约485文件通过；中文注释自测6/6、差异检查0失败 / 0警告，`git diff --check`通过。

## 下一步

1. 提交P7-3 PR并确认远程 `main` 含合并提交。
2. 从最新 `origin/main` 承接P8-1规则治理状态机。
3. 继续按OpenSpec顺序承接线2、线3、线4未完成任务与真实工作流验收。

文档只维护本文件、backlog、域简报和对应卡片，不新增施工说明。
