# 会话接力

## 当前执行

- 分支：`codex/platform-inheritance-cleanup`；基线：`origin/main` = `3582833d`。
- 线1统一承接全部任务；项目未上线，不保留旧并行模型或兼容层。

## 当前状态

- PR #513 已合并，平台继承批量解析、五方言索引与全门禁进入 `main`。
- 平台继承稳定要求已收敛到唯一主规格；已完成变更目录不再保留。
- 核查发现术语包、专病包仍保留专用 DTO/API/状态映射，属于待清除的旧外观。

## 下一步

1. 删除专用包模型与兼容 API，统一由 `KnowledgePackage` + `PackageItem` 筛选视图承载。
2. 复验术语、路径、配置包页面与完整门禁后提交 PR。
