# 会话接力

## 唯一执行组织

- 当前分支：`codex/d6-graph-stabilization`
- 基线：`origin/main` = `cba27783`
- 线1统一承接全部任务；线2 / 线3 / 线4不再独立开发、抢合并或维护重复实现。
- 项目未上线：只保留唯一模型、唯一接口、唯一主流程，不新增兼容层。

## 当前状态

- D0–D5及线2 / 线3 / 线4在D6之前的承接项已合入主线。
- D6已合入：`OPT-10`、`DOMCHK-01`、`DEVCON-01`、`PROV-01`。
- `GRAPH-01`已完成实现与本地验收，待提交、PR、CI和合并。
- D6仅余`AIFLOW-01`；域级验收仍为`pending`。

## 当前证据

- 前端全量：77个测试文件、518项测试通过；ESLint、Stylelint、格式、类型检查和生产构建通过。
- 真实浏览器：14项登录域E2E和2项GRAPH E2E通过；正式API创建并发布知识、重建投影，专科专家可查询/下钻且无重建权限；桌面与390px移动端无页面级横向溢出。
- 后端全量1789项通过，0失败 / 0错误 / 0跳过；H2、PostgreSQL、Oracle均迁移至V97并完成幂等复跑。
- T-GATE：38项门禁自测、真实性1289文件、配置边界1214文件、迁移规约485文件通过；中文注释0 fail / 0 warn。

## 下一步

1. 完成`GRAPH-01`门禁、提交、PR、CI与合并。
2. 从最新`origin/main`继续`AIFLOW-01`。
3. 完成D6域级验收。

文档只维护本文件、backlog、域简报和对应卡片，不新增施工说明。
