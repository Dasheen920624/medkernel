# 会话接力

## 当前执行

- 分支：`codex/third-party-api-66`；基线：`origin/main` = `6ac0c768`（PR #509 / 5.8 已合入）。
- 线1统一承接全部任务；线2 / 线3 / 线4不再独立开发或维护重复实现。
- 项目未上线：保留唯一模型、唯一接口、唯一主流程，不做旧方案兼容层。

## 6.6 状态

- 已统一第三方有效包查询、上下文写入、覆盖管理、包分发与对账 API；作用域维度、历史生效时间、来源层级、内容摘要、权限和幂等合同均走现有权威主流程。
- 开发者控制台已展示 `v1` 契约、OpenAPI 与字段契约；登录、安全设置门禁、桌面与 `390×844` 窄屏均完成真实浏览器验收。
- 本地证据：后端 `2105` 用例全绿；前端 `83` 文件 / `594` 用例与生产构建全绿；OpenSpec 全仓 strict、真实性 `1527` 文件、配置边界 `1440` 文件、迁移规约 `550` 文件、门禁自测 `38/38`、中文注释和空白检查均通过。
- 待补：提交、远端 CI、合并。

## 下一步

1. 完成 6.6 PR、CI 与合并。
2. 从最新 `main` 继续 6.7 entitlement 授权层。
