# AIFLOW-01 · AI 工作流页

> 读卡前置：[核心 CONSTITUTION](../../CONSTITUTION.md) + [D6 域简报](_brief.md) + [体验契约](../../EXPERIENCE_CONTRACT.md)。
> 迁移来源（覆盖矩阵锚点）：详规 AI 工作流 · 核心 §11 B0 先于模型 · 体验规范 §3。
> 实化映射：占位 `D6-PAGE-AI 工作流` → 本卡 **AIFLOW-01**。

## 身份
- 卡 ID：AIFLOW-01（页面卡；= backlog `D6-PAGE-AI 工作流` 实化）
- 域：D6 高级工具
- 关联场景：S15 AI 验证与验收（技术侧）
- 依赖卡：[BASE-08](../D0/BASE-08.md)/[BASE-10](../D0/BASE-10.md) · [INFRA-09](../D1/INFRA-09.md) · wave2 LLM-*/AIK-*（编排，后置）
- 工作量：2d
- owner / reviewer：Codex / PR审阅人（owner ≠ reviewer）

## 目标
把 AI 工作流页收敛为唯一的**真实能力目录与状态查看页**：读取当前组织已登记能力、路由、数据保护、结构约束和降级状态；**本期不做编排/执行**（留第二波 wave2），不在前端伪造能力或状态。

## 实现状态（2026-06-07）
旧模型执行与配置控制台已删除；页面仅消费`GET /api/v1/model-capabilities/status`，展示真实能力状态与详情。未新增表、迁移、兼容层或第二套模型；编排、执行和策略写入仍归wave2。

## 功能要求（原子可测条目）
- [x] FR-1 能力目录：从真实状态接口列出当前组织已登记能力，空数据不补本地默认项。
- [x] FR-2 查看详情：只读查看能力代码、路由、脱敏、租户配置、结构约束和状态说明。
- [x] FR-3 编排后置：**本期不提供编排、执行、编辑或策略写入入口**。
- [x] FR-4 诚实降级：展示`B0`、`MODEL_DISABLED`、`NOT_AVAILABLE`和部分能力不可用状态。
- [x] FR-5 六态 + RBAC：加载、空、错误、无权限、部分成功、正常齐全；数据按`OrgContext`，不入临床主菜单。

## 接口契约 / 页面契约
### 接口契约（引擎/API 卡）
- `GET /api/v1/model-capabilities/status`：返回当前租户能力目录、路由策略、脱敏策略、结构约束、配置状态和B0降级原因。
- 编排、执行和策略写入接口不属于本卡页面契约。
### 页面契约（页面卡）
- 路由元数据：sectionKey `knowledge-configuration` / placement `expert` / menuKey `ai-workflows` / menuLabel `智能工作流` / path `/advanced/ai-workflows` / requiredPermissions `menu.ai-workflows` + `llm.read`。
- 可见角色：实施工程师、信息运维、医务处、平台/集团/医院管理员；其他角色直接访问显示无权限。
- 结构：PageShell + 状态摘要 + 能力表格 + 行内详情 + 六态。
- 只读页无主按钮，仅保留刷新图标；无筛选、无编辑入口。
- 五维 RBAC：菜单 / 读取动作 / 数据（org）/ 能力资产 / 环境。
- 样式：仅引用 [BASE-10](../D0/BASE-10.md) token + [体验契约](../../EXPERIENCE_CONTRACT.md)；禁硬编码 hex/px。

## 数据与迁移
N·A —— 页面卡不落库；复用现有模型能力定义、租户策略与状态查询。

## 视角清单（11 视角逐条）
1. 产品架构：AI能力目录与运行状态的唯一只读入口（编排wave2）。
2. 产品体验：摘要、列表、详情和六态清晰；桌面与移动端可读。
3. 系统与数据架构：查看列表 P95 ≤1s。
4. 临床医疗安全：N·A（技术工具）。
5. 知识与数据治理：N·A。
6. 安全合规与监管：查看留审计（[BASE-04](../D0/BASE-04.md)）。
7. 集团化与多租户治理：按 `OrgContext` 作用域。
8. 集成与互操作：编排引擎（Dify 等）对接留 wave2。
9. 运维 / SRE / 国产化：内网慢场景骨架。
10. 质量与真实性审计：★只读真实状态，不前端假编排、假执行或默认能力；无演示路由（[INFRA-09](../D1/INFRA-09.md)）。
11. AI / 模型治理与可降级：★B0先于模型；按能力显示路由、脱敏、结构约束与降级原因。

## 适用不变量
- 命中核心约束：**核心 §11 B0 先于模型（编排后置）** · **铁律 #1 真实性** · **技术对象不入主路径**。
- 本卡落点：把 AI 工作流页收敛为真实能力状态查看页，编排/执行留第二波且不提供假入口。

## 验收 + 验证
- [x] AC-1（FR-1/2）：真实能力目录和详情可查看，空数据诚实。
- [x] AC-2（FR-3/4）：无编排、执行、编辑或写入入口；不可用诚实降级。
- [x] AC-3（FR-5）：六态齐全；实际RBAC生效，不入临床主菜单。
- 关联 A1–A9 剧本：A9 AI 工作流查看。
- T-GATE：前端真实性门禁全绿（no-page-mock、无假编排/执行）。
- B0 验收：★无外部模型仍可查看能力目录和B0状态，不伪造能力。

## 完工证据
- 代码：`pages/advanced/AiWorkflows`、`useModelCapabilitiesStatus`和统一路由权限。
- 测试：真实状态、加载/空/错误/无权限/部分成功/正常、RBAC、无写入口及桌面/390px真实API E2E。
- 数据/迁移：复用现有模型能力定义与状态接口，无新增迁移。
- 审计员签字：PR审阅人（owner ≠ reviewer）。
