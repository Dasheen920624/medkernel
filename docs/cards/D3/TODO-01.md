# TODO-01 · 待办中心页

> 读卡前置：[核心 CONSTITUTION](../../CONSTITUTION.md) + [D3 域简报](_brief.md) + [体验契约](../../EXPERIENCE_CONTRACT.md)。
> 迁移来源（覆盖矩阵锚点）：详规 S8 临床嵌入运行 · 详规 待办协同 · 体验规范 §3 低打扰。
> 实化映射：占位 `D3-PAGE-待办中心` → 本卡 **TODO-01**。

## 身份
- 卡 ID：TODO-01（页面卡；= backlog `D3-PAGE-待办中心` 实化）
- 域：D3 临床运行
- 关联场景：S8 临床嵌入运行
- 依赖卡：[SVC-CLINICAL-03](SVC-CLINICAL-03.md)（待办聚合后端）· [MED-C3](MED-C3.md)（安全复核任务）· [BASE-08](../D0/BASE-08.md)/[BASE-10](../D0/BASE-10.md) · [API-13](../D0/API-13.md) · [INFRA-09](../D1/INFRA-09.md)
- 工作量：3d
- owner / reviewer：待派单（owner ≠ reviewer）

## 目标
把待办中心页**真实化**：统一呈现多源待办（CDSS 复核/随访/安全复核），按责任人/截止/来源闭环处理，**不前端造待办**，低打扰。

## 现状（搬迁时核查 2026-05-30，以 `frontend/src` 为准）
页面**已存在待真实化**：`pages/clinical/WorkflowTodos.tsx`（路由 `/workflow/todos` 已注册 `app/router.tsx`）。本卡＝去占位/mock + 接 [SVC-CLINICAL-03](SVC-CLINICAL-03.md) 统一待办 API + 六态/五维 RBAC 齐全。

## PR1 实施边界（2026-06-04，本地完整验证）
- 已实现：`WorkflowTodos.tsx` 删除旧“接口尚未接入”占位，改为消费 `useWorkflowTodos` / `useCompleteWorkflowTodo`，展示来源、患者、责任人、截止、优先级、状态、来源跳转和完成弹窗。
- 已覆盖：随访任务、安全复核、推荐卡三类真实来源；安全复核与高风险待办在后端分页层置顶，前端当前页再次按同口径排序；完成说明必填并通过后端持久化；列表请求使用后端 1 基分页并支持 `patientId` 过滤。
- 未冒领：转交操作、护理 / 报告 / 床旁知识来源展示仍需后续 PR 接入真实来源；当前页面不造待办，不用本地示例兜底。

## PR2 实施边界（2026-06-04，PR #371 已合并）
- 已实现：待办中心新增转交入口，弹窗收集接收人、接收角色和转交说明，调用 `POST /api/v1/engine/workflow/todos/{todoId}/transfer` 后刷新后端分页数据；转交说明必填，不能在页面本地伪改状态。
- 已覆盖：`PENDING` / `IN_PROGRESS` 待办可转交为 `TRANSFERRED`，后端持久化 `transferredTo` / `transferReason` 并更新责任人 / 角色；列表可展示已转交给谁和转交原因，仍保留安全复核 / 高风险排序保护。
- 未冒领：护理 / 报告 / 床旁知识真实来源仍未接入；转交不代表外发通知已投递。

## PR3 实施边界（2026-06-04，本地验证）
- 已实现：完成 / 转交待办后由后端生成 `WORKFLOW_TODO` 站内通知，前端待办中心仍只通过后端状态刷新，不在浏览器本地追加通知。
- 已覆盖：完成回执低打扰通知发给当前责任人；转交通知按待办优先级发给新责任人，保留来源链接与 traceId，供通知中心继续处理已读回执。
- 未冒领：站内通知不等于短信 / 邮件 / Webhook 或院内消息已投递；护理 / 报告 / 床旁知识来源仍待真实上游接入。

## PR5 实施边界（2026-06-04，本地验证）
- 已实现：待办中心可展示由真实推荐卡类型细分出的护理任务、报告解读、床旁知识来源；后端按 `recommendation_card.card_type` 和报告触发语义投影 `NURSING_TASK` / `REPORT_INTERPRETATION` / `BEDSIDE_KNOWLEDGE`，前端来源列与筛选项均显示中文。
- 已覆盖：推荐卡派生来源按 `cardId` 去重，避免历史普通推荐卡待办与新细分来源重复；页面不暴露 `FOLLOWUP_TASK`、`NURSING_TASK` 等裸枚举，仍只依赖统一待办 API 刷新。
- 未冒领：本 PR 不接护理站、LIS / PACS 或外部床旁知识系统直连；若没有落库为推荐卡类型，不在前端造来源待办。外发通知投递仍未接通。

## PR7 实施边界（2026-06-04，本地验证）
- 已实现：待办中心查询触发真实来源同步时，后端为新入库或历史缺通知的开放待办幂等派生 `WORKFLOW_TODO` “待办待处理”通知，去重键为 `todo:<todoId>:created`。
- 已覆盖：随访、安全复核、推荐卡及护理 / 报告解读 / 床旁知识细分来源均复用同一待办创建通知逻辑；页面仍只刷新统一待办 API，不在浏览器本地插入通知。
- 未冒领：本 PR 不新增页面按钮，不声明外部护理站、LIS / PACS 或床旁知识系统已接通；外发只登记补偿状态，不代表真实送达。

## PR8 实施边界（2026-06-05，本地验证）
- 已实现：待办中心默认查询由后端收敛为当前用户责任待办 + 未指定个人责任人的组织范围待办，不再因页面空 `assigneeId` 参数返回同租户所有个人待办。
- 已覆盖：完成 / 转交前校验待办可见性；他人个人待办返回 `notFound` 且不保存完成 / 转交状态、不派生站内通知或外发补偿。
- 未冒领：页面仍不新增科室树筛选；本 PR 未实现真实科室 / 组织闭包级待办池，只保护当前表中已绑定个人责任人的串读 / 误处理风险。

## PR9 实施边界（2026-06-05，本地验证）
- 已实现：待办中心触发的完成 / 转交操作在后端真实保存后写入统一审计，审计目标为 `workflow_todo`，动作 `UPDATE`，记录流转前后状态、责任人、traceId 与转交说明。
- 已覆盖：越权处理他人个人待办仍返回 `notFound`，不保存状态、不派生站内通知、不登记外发补偿、不写成功审计；审计快照不包含患者 ID、待办标题或摘要正文。
- 未冒领：页面无新增本地状态机或假审计展示；短信 / 邮件 / Webhook / 院内消息真实投递仍未接通，科室 / 组织闭包级待办池仍待后续。

### PR10 组织闭包级待办池（2026-06-04）
- 已实现：待办中心默认查询由后端进一步收敛为当前用户责任待办 + 未指定个人责任人且属于当前 `OrgContext` 祖先 / 后代闭包内的组织待办；`orgUnitId` 为空的历史 / 租户级组织待办继续可见。
- 已覆盖：兄弟科室组织型待办不会在默认列表出现，完成 / 转交前单条可见性同样走组织闭包查询；越权路径不保存状态、不派生通知、不登记外发补偿、不写成功审计。
- 未冒领：页面不新增科室树筛选，不声明护理站、LIS / PACS、独立床旁知识系统或外发短信 / 邮件 / Webhook / 院内消息已接通；达梦 / 人大金仓真实运行继续后置。

### PR11 显式组织范围筛选（2026-06-05，本地验证）
- 已实现：待办中心新增“组织范围”筛选，选项来自真实 `useOrgUnits` 组织 API；选择科室 / 专病后把 `orgUnitId` 传给 `useWorkflowTodos`，由后端按组织闭包子树和当前可见性共同收窄。
- 已覆盖：页面只改变服务端查询参数，不在浏览器本地过滤或造待办；默认不选择组织时保持 PR10 的当前用户 + 组织闭包默认视图。
- 未冒领：本 PR 不新增护理站、LIS / PACS、独立床旁知识系统直连，不展示外部投递回执；达梦 / 人大金仓真实运行继续后置。

### PR13 来源跳转安全降级（2026-06-05，本地验证）
- 已实现：待办中心的“打开来源”仅在后端 deepLink 为站内应用路径时渲染；外部 URL、协议相对 URL、`javascript:` 与包含空白 / 控制字符或反斜杠的 deepLink 均降级为“来源暂不可跳转”。
- 已覆盖：安全站内 deepLink 仍可跳转来源页；不可安全跳转时待办行仍可完成 / 转交，不在浏览器本地改写为伪路由。
- 未冒领：本 PR 不接护理站、LIS / PACS、独立床旁知识系统，也不验证外部来源系统可达性；只保护统一待办页的来源跳转动作。

### PR14 协同来源证据呈现（2026-06-05，本地验证）
- 已实现：待办行在标题 / 摘要下展示“来源对象”与存在时的“追踪链路”，直接使用统一待办 API 返回的 `sourceId` 与 `traceId`。
- 已覆盖：来源证据与来源类型、患者、责任人、截止、优先级、状态和操作同页呈现；缺 `traceId` 的待办不在页面生成假追踪值。
- 未冒领：本 PR 不新增护理站、LIS / PACS、独立床旁知识系统直连，也不校验外部 deepLink 可达；页面只展示已落库协同来源证据。

### PR15 缺失来源跳转诚实提示（2026-06-05，本地验证）
- 已实现：待办行在后端未返回 `deepLink` 时显示“来源未提供跳转”，与有 `deepLink` 但不可安全打开时的“来源暂不可跳转”区分。
- 已覆盖：无来源跳转的待办仍展示来源对象与追踪链路，完成 / 转交按钮保持后端主链路，不在前端生成伪来源路由。
- 未冒领：本 PR 不接护理站、LIS / PACS 或外部床旁知识系统，也不验证外部来源系统可达性；仅补待办页缺失 deepLink 的诚实状态。

### PR16 缺失追踪链路诚实提示（2026-06-05，本地目标红绿）
- 已实现：待办行在后端未返回 `traceId` 时显示“追踪链路未提供”，与真实 `traceId` 的追踪链路展示区分。
- 已覆盖：无追踪链路的待办仍展示来源对象、来源类型、患者 / 就诊上下文和来源跳转状态，完成 / 转交按钮继续走后端主链路。
- 未冒领：本 PR 不新增审计查询页，不在前端生成伪 traceId，不声明外部来源系统可达；仅补待办页缺失 traceId 的诚实状态。

### 页面收口核查（2026-06-05，本地回归）
- 已覆盖：统一待办页已接真实 `useWorkflowTodos` / 完成 / 转交 API，多源来源、责任人、截止、优先级、安全复核置顶、来源对象、追踪链路、来源跳转安全降级、缺失 deepLink / traceId 诚实提示和组织范围筛选均在页面与测试中回归。
- RBAC 边界：前端路由具备 `menu.workflow-todos` 登录菜单保护，完成 / 转交前的可见性、本人 / 组织闭包数据范围和越权不保存以后端工作流服务 / 控制器权限测试为准。
- 未冒领：仍不声明护理站、LIS / PACS、独立床旁知识系统或短信 / 邮件 / Webhook / 院内消息真实连接器已接通；页面只消费统一待办 API 与已落库证据。

## 功能要求（原子可测条目）
- [x] FR-1 统一待办：列多源待办（来源/责任人/截止/优先级），数据真实（[API-13](../D0/API-13.md) 分页）。
- [x] FR-2 安全优先：安全复核任务（[MED-C3](MED-C3.md)）置顶高优先、不可忽略。
- [x] FR-3 闭环处理：待办流转到完成/转交，状态机闭环并写统一审计。
- [x] FR-4 跳转来源：每条待办可跳来源页（提醒/患者/路径；仅安全站内 deepLink 显示为可点击来源，异常链接与缺失链接均诚实提示；页面展示来源对象与追踪链路，缺失 traceId 时显示诚实缺失状态）。
- [x] FR-5 六态 + 五维 RBAC：齐全；按责任人/科室作用域；数据按 `OrgContext`。（当前用户 + 组织闭包范围默认后端保护已覆盖；页面显式组织范围筛选已接真实组织 API）

## 接口契约 / 页面契约
### 接口契约（引擎/API 卡）
N·A —— 消费 [SVC-CLINICAL-03](SVC-CLINICAL-03.md) 待办 API。
### 页面契约（页面卡）
- 路由元数据：sectionKey `clinical-run` / menuKey `workflow-todos` / menuLabel `待办中心` / path `/workflow/todos` / requiredPermissions 待办处理 / requiredRoles 临床医生·护理·科主任。
- 结构：PageShell（[BASE-08](../D0/BASE-08.md)）+ 待办列表（优先级排序、安全置顶）+ 处理抽屉 + 六态。
- 主按钮 ≤1（完成待办）/ 默认筛选 ≤3（我的/今日/高优先）/ 默认角色视图（本人）。
- 五维 RBAC：菜单 / 动作（完成/转交）/ 数据（org/本人）/ 资产 / 环境。
- 样式：仅引用 [BASE-10](../D0/BASE-10.md) token + [体验契约](../../EXPERIENCE_CONTRACT.md)；禁硬编码 hex/px。

## 数据与迁移
N·A —— 页面卡不落库；消费 [SVC-CLINICAL-03](SVC-CLINICAL-03.md) 后端。

## 视角清单（11 视角逐条）
1. 产品架构：临床各源任务的"统一处理入口"。
2. 产品体验：★一处理多源、安全置顶、低打扰；国产浏览器可读。
3. 系统与数据架构：待办分页 P95 ≤1s；优先级排序稳定。
4. 临床医疗安全：★安全复核任务（[MED-C3](MED-C3.md)）高优先不漏。
5. 知识与数据治理：待办来源可追溯。
6. 安全合规与监管：待办完成/转交留审计（[BASE-04](../D0/BASE-04.md)）。
7. 集团化与多租户治理：按责任人/科室 + `OrgContext` 作用域。
8. 集成与互操作：跳转来源页深链。
9. 运维 / SRE / 国产化：内网慢场景骨架。
10. 质量与真实性审计：★无前端造待办、去重正确、闭环可审计；无演示路由（[INFRA-09](../D1/INFRA-09.md)）。
11. AI / 模型治理与可降级：N·A（确定性页面；智能摘要为 wave2 挂点）。

## 适用不变量
- 命中核心约束：**铁律 #1 真实性** · **核心 §13 低打扰** · **§5 状态机闭环** · **§9 多租户作用域**。
- 本卡落点：把待办中心页变为接真实多源待办、安全置顶、闭环可审计的处理入口。

## 验收 + 验证
- [x] AC-1（FR-1/2）：多源待办真实；安全复核置顶。
- [x] AC-2（FR-3/4）：闭环处理留审计；跳转来源正确，异常 deepLink 不暴露为可点击外部 / 危险链接，缺失 deepLink 显示“来源未提供跳转”；来源对象与追踪链路来自真实 API 字段，缺失 traceId 不伪造追踪号。
- [x] AC-3（FR-5）：六态齐全；越权不可处理他人待办。
- 关联 A1–A9 剧本：A6 协同待办 · A8 安全复核。
- T-GATE：前端真实性门禁全绿（no-page-mock、无造待办、无演示路由）。
- B0 验收：N·A（确定性页面）。

## 完工证据
- 代码 permalink：`frontend/src/pages/clinical/WorkflowTodos.tsx` 消费 [SVC-CLINICAL-03](SVC-CLINICAL-03.md) 统一待办 API，完成 / 转交 / 组织筛选 / 来源跳转均走后端事实。
- 测试：`npm test -- src/pages/clinical/WorkflowTodos.test.tsx` 覆盖真实 API 渲染、组织范围服务端查询、多源中文来源、安全复核置顶、安全 deepLink、危险 / 缺失 deepLink 诚实降级、缺失 traceId 诚实提示、完成与转交后端持久化；同轮 D3 页面组 8 文件 / 65 用例通过。
- 后端证据：`mvn -q -Dtest=WorkflowCollaborationServiceTest,WorkflowTodoRepositoryTest test` 属于本轮后端目标组并退出码 0；越权不可见 / 不保存、组织闭包、审计和通知派生以后端服务测试为准。
- 审计员签字：@<reviewer>（owner ≠ reviewer）。
